[project]
name = "optimization"
version = "0.1.0"
description = "模型优化示例项目"
requires-python = ">=3.11"
dependencies = ["numpy>=1.26.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]