[project]
name = "ml-basics"
version = "0.1.0"
description = "机器学习基础概念示例项目"
requires-python = ">=3.11"
dependencies = ["numpy>=1.26.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]