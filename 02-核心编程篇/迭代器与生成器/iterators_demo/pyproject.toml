[project]
name = "iterators-demo"
version = "0.1.0"
description = "Python 迭代器与生成器示例项目"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]