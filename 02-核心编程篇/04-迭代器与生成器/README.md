# 迭代器与生成器

本章讲解 Python 迭代器与生成器的完整知识体系，包括迭代原理、自定义迭代器、生成器语法和 itertools 模块。

---

## 章节导航

| 章节 | 文件 | 主题 |
|------|------|------|
| 01 | [01-迭代基础.md](./01-迭代基础.md) | 可迭代对象、迭代器、for 循环原理 |
| 02 | [02-自定义迭代器.md](./02-自定义迭代器.md) | 迭代器协议、自定义迭代器类 |
| 03 | [03-生成器基础.md](./03-生成器基础.md) | yield 关键字、生成器表达式 |
| 04 | [04-生成器高级特性.md](./04-生成器高级特性.md) | send()、throw()、close() |
| 05 | [05-itertools模块.md](./05-itertools模块.md) | 无限迭代器、组合迭代器 |
| 06 | [06-异步生成器.md](./06-异步生成器.md) | async yield、AsyncGenerator、异步迭代协议 |

---

## 核心概念

```
┌─────────────────────────────────────────────────────────────┐
│                    迭代概念关系图                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   可迭代对象 (Iterable)                                     │
│   ───────────────────                                       │
│   • 实现了 __iter__() 方法                                  │
│   • 例如：list, tuple, str, dict, set, range               │
│                                                             │
│         ▼ iter() 获取                                       │
│                                                             │
│   迭代器 (Iterator)                                         │
│   ───────────────                                           │
│   • 实现了 __iter__() 和 __next__() 方法                    │
│   • 每次返回一个值，直到耗尽                               │
│                                                             │
│         ▼ yield 关键字                                      │
│                                                             │
│   生成器 (Generator)                                        │
│   ───────────────                                           │
│   • 使用 yield 的函数                                       │
│   • 惰性求值，内存友好                                     │
│                                                             │
│         ▼ async def + yield                                 │
│                                                             │
│   异步生成器 (AsyncGenerator)                               │
│   ─────────────────────────                                 │
│   • async def 函数中使用 yield                              │
│   • 异步惰性求值，用 async for 遍历                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 核心语法

```python
# 迭代器协议
class MyIterator:
    def __iter__(self):
        return self
    def __next__(self):
        raise StopIteration

# 生成器函数
def my_generator():
    yield 1
    yield 2

# 生成器表达式
gen = (x ** 2 for x in range(10))
```