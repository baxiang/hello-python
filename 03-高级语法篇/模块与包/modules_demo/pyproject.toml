[project]
name = "modules-demo"
version = "0.1.0"
description = "Python 模块与包示例项目"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]