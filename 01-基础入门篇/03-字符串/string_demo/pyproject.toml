[project]
name = "string-demo"
version = "0.1.0"
description = "Python 字符串操作示例项目"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]