# Python 学习大纲

本教程共 39 章，分九大篇章，从零基础入门到服务端开发、机器学习、深度学习全栈覆盖。

---

## 课程全景

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           九篇章学习路径                                     │
├──────────────┬──────────────┬──────────────┬───────────────────────────────┤
│   基础篇     │   核心篇     │   进阶篇     │         实战篇                 │
│   第 1-9 章   │   第 10-16 章 │   第 17-18 章 │         第 19 章               │
├──────────────┼──────────────┼──────────────┼───────────────────────────────┤
│ 01 简介与环境 │ 10 函数       │ 17 正则表达式 │ 19 项目实践                    │
│ 02 变量与类型 │ 11 模块与包   │ 18 并发编程   │   • 天气查询                  │
│ 03 运算符     │ 12 文件操作   │              │   • 文件重命名                 │
│ 04 流程控制   │ 13 面向对象   │              │   • 待办事项                  │
│ 05 字符串     │ 14 错误与异常 │              │   • 网页爬虫                  │
│ 06 列表       │ 15 迭代器生成器│             │   • CSV 分析                  │
│ 07 元组       │ 16 装饰器闭包 │              │                               │
│ 08 字典       │              │              │                               │
│ 09 集合       │              │              │                               │
└──────────────┴──────────────┴──────────────┴───────────────────────────────┘

┌──────────────┬──────────────┬───────────────────────────────────────────────┤
│   专题篇     │   服务端篇   │              AI 方向                            │
│   第 20-23 章 │   第 24-29 章 │              第 30-39 章                       │
├──────────────┼──────────────┼───────────────────────────────────────────────┤
│ 20 uv 包管理器│ 24 Web 基础   │ 30-34 机器学习篇                              │
│ 21 类型提示   │ 25 Flask 上手 │   • 机器学习基础                              │
│ 22 列表推导式 │ 26 Flask 进阶 │   • 数据预处理                                │
│ 23 asyncio 高 级│ 27 Flask 高级  │   • 监督学习 (回归/分类/决策树)                │
│              │ 28 FastAPI 详解│   • 无监督学习 (聚类/PCA)                      │
│              │ 29 服务端实战  │   • 模型评估与优化                            │
│              │              │                                               │
│              │              │ 35-39 神经网络与深度学习篇                      │
│              │              │   • 神经网络基础                                │
│              │              │   • PyTorch 入门                                │
│              │              │   • CNN 卷积神经网络                           │
│              │              │   • RNN 循环神经网络                           │
│              │              │   • 深度学习实战项目                           │
└──────────────┴──────────────┴───────────────────────────────────────────────┘
```

### 完整技术路线图

```
基础阶段                    核心阶段                    进阶阶段
─────────                  ──────────                  ──────────
第一篇                     第二篇                      第三篇
基础入门       ──→         核心编程        ──→         进阶提升
(01-09)                    (10-16)                     (17-18)
    │                         │                          │
    └─────────────────────────┴──────────────────────────┘
                              │
                              ▼
    ┌─────────────────────────────────────────────────────────────────────┐
    │                      第四篇 项目实践篇 (19)                          │
    │                  5 个完整项目综合运用                                │
    └─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
    ┌─────────────────────────────────────────────────────────────────────┐
    │                      第五篇 Python 工具箱                            │
    │                 uv + 标准库速查 + 常用库                             │
    └─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
    ┌─────────────────────────────────────────────────────────────────────┐
    │                      第六篇 专题深入篇 (20-23)                       │
    │            类型提示  /  列表推导式  /  asyncio 高级编程              │
    └─────────────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│ 第七篇 服务端 │    │ 第八篇 机器  │    │ 第九篇 神经  │
│   编程篇      │    │   学习篇     │    │   网络篇     │
│   (24-29)     │    │   (30-34)    │    │   (35-39)    │
│               │    │               │    │               │
│ • Web 基础    │    │ • scikit-    │    │ • PyTorch    │
│ • Flask       │    │   learn      │    │ • CNN        │
│ • FastAPI     │    │ • 监督学习   │    │ • RNN        │
│ • REST API    │    │ • 无监督学习 │    │ • 项目实战   │
│ • WebSocket   │    │ • 模型优化   │    │               │
└───────────────┘    └───────────────┘    └───────────────┘
        │                     │                     │
        └─────────────────────┴─────────────────────┘
                              │
                              ▼
                 ┌────────────────────────┐
                 │     全栈能力打通        │
                 │  Web + AI + 数据科学    │
                 └────────────────────────┘
```

---

## 第一篇：基础入门篇（第 01-09 章）

### 第 1 章 - Python 简介与环境搭建

**文件：** [01-Python 简介与环境搭建.md](./01-Python 简介与环境搭建.md)

| 知识点 | 说明 |
|--------|------|
| Python 是什么 | 语言特点、应用场景、版本选择 (Python 3.11+) |
| 环境安装 | uv 安装、Python 3.11+ 配置、虚拟环境 |
| 第一个程序 | `print("Hello, World!")`、运行方式 |
| REPL 交互 | 交互式解释器的使用、IPython |
| 代码规范 | PEP 8 基础规范、注释写法、命名约定 |
| 编辑器配置 | VS Code + Python 插件配置 |

---

### 第 2 章 - 变量与数据类型

**文件：** [02-变量与数据类型.md](./02-变量与数据类型.md)

| 知识点 | 说明 |
|--------|------|
| 变量赋值 | 命名规则、多重赋值、变量交换 |
| 整数 int | 整数运算、进制表示 (0b/0o/0x)、大整数 |
| 浮点数 float | 精度问题、科学计数法、`decimal` 模块 |
| 字符串 str | 基础字符串操作 (详见第 5 章) |
| 布尔值 bool | True/False、与其他类型的关系、真值测试 |
| 空值 None | None 的含义与使用场景 |
| 类型转换 | `int()`/`float()`/`str()`/`bool()` |
| `type()` 与 `isinstance()` | 类型检查的两种方式 |

---

### 第 3 章 - 基本运算符

**文件：** [03-基本运算符.md](./03-基本运算符.md)

| 知识点 | 说明 |
|--------|------|
| 算术运算符 | `+` `-` `*` `/` `//` `%` `**` |
| 比较运算符 | `==` `!=` `>` `<` `>=` `<=` |
| 逻辑运算符 | `and` `or` `not`、短路求值 |
| 赋值运算符 | `=` `+=` `-=` `*=` 等复合赋值 |
| 位运算符 | `&` `|` `^` `~` `<<` `>>` |
| 成员运算符 | `in` `not in` |
| 身份运算符 | `is` `is not`、与 `==` 的区别 |
| 运算符优先级 | 优先级顺序表、括号改变优先级 |

---

### 第 4 章 - 流程控制

**文件：** [04-流程控制.md](./04-流程控制.md)

| 知识点 | 说明 |
|--------|------|
| if 语句 | if / elif / else 结构、嵌套 if |
| 三元表达式 | `x if 条件 else y` |
| match 语句 | Python 3.10+ 模式匹配 (结构匹配、守卫条件) |
| for 循环 | 遍历序列、`range()`、`enumerate()` |
| while 循环 | 条件循环、无限循环、`break` |
| break / continue | 跳出与跳过、`else` 子句 |
| 嵌套循环 | 多层循环、`break` 跳出多层、性能注意 |

---

### 第 5 章 - 字符串

**文件：** [05-字符串.md](./05-字符串.md)

| 知识点 | 说明 |
|--------|------|
| 字符串创建 | 单引号/双引号/三引号/原始字符串 (r-prefix)/f-string |
| 索引与切片 | `s[0]`、`s[1:5]`、`s[::-1]`、负索引 |
| 常用方法 | `upper/lower/strip/split/join/replace/find/count` |
| 字符串格式化 | f-string (推荐)、`.format()`、`%` 格式化 |
| 字符串判断 | `startswith/endswith/isdigit/isalpha/isalnum` |
| 转义字符 | `\n` `\t` `\\` `\"` `\r` |
| 字符串编码 | Unicode、UTF-8、`encode()`/`decode()` |
| 字节与字节数组 | `bytes`、`bytearray` 基础 |

---

### 第 6 章 - 列表

**文件：** [06-列表.md](./06-列表.md)

| 知识点 | 说明 |
|--------|------|
| 列表创建 | `[]` 字面量、`list()` 构造、`*` 重复 |
| 索引与切片 | 正向/反向索引、切片赋值、步长 |
| 常用方法 | `append/extend/insert/remove/pop/clear/index/count` |
| 排序 | `sort()` (原地)、`sorted()` (新列表)、`key` 参数、`reverse` |
| 列表推导式 | `[x for x in ... if ...]` (详见第 22 章) |
| 嵌套列表 | 二维列表的创建与遍历、矩阵操作 |
| 浅拷贝与深拷贝 | `copy()` 与 `copy.deepcopy()` 的区别 |
| 列表性能 | 列表操作的复杂度分析 |

---

### 第 7 章 - 元组

**文件：** [07-元组.md](./07-元组.md)

| 知识点 | 说明 |
|--------|------|
| 元组创建 | `()` 字面量、单元素元组的逗号、`tuple()` 构造 |
| 不可变特性 | 元组不可修改的原因与用途、内存优势 |
| 解包 | 多重赋值、`*` 扩展解包、嵌套解包 |
| 具名元组 | `collections.namedtuple`、可读性提升 |
| 元组与列表对比 | 何时用元组、何时用列表、性能对比 |

---

### 第 8 章 - 字典

**文件：** [08-字典.md](./08-字典.md)

| 知识点 | 说明 |
|--------|------|
| 字典创建 | `{}` 字面量、`dict()` 构造、`zip()` 组合 |
| 访问与修改 | `d[key]`、`d.get(key, default)`、`setdefault()` |
| 常用方法 | `keys/values/items/update/pop/clear/copy` |
| 遍历字典 | 遍历键、值、键值对、`items()` 解包 |
| 字典推导式 | `{k: v for k, v in ...}` |
| 嵌套字典 | 多层字典的访问与修改、默认字典 `defaultdict` |
| 有序性 | Python 3.7+ 字典保持插入顺序 |
| 字典性能 | O(1) 查找、哈希表原理简介 |

---

### 第 9 章 - 集合

**文件：** [09-集合.md](./09-集合.md)

| 知识点 | 说明 |
|--------|------|
| 集合创建 | `{1, 2, 3}`、`set()` 构造、空集合 `set()` |
| 集合运算 | 交集 `&`、并集 `|`、差集 `-`、对称差 `^` |
| 常用方法 | `add/remove/discard/pop/clear/update` |
| 集合判断 | 子集 `<` / `<=`、超集 `>` / `>=`、`in` 操作 |
| frozenset | 不可变集合、作为字典的键 |
| 集合的典型应用 | 去重、成员检测 (O(1) 查询)、集合运算 |

---

## 第二篇：核心编程篇（第 10-16 章）

### 第 10 章 - 函数

**文件：** [10-函数.md](./10-函数.md)

| 知识点 | 说明 |
|--------|------|
| 函数定义 | `def`、文档字符串、返回值、类型注解 |
| 参数类型 | 位置参数、关键字参数、默认参数 |
| 可变参数 | `*args` (位置)、`**kwargs` (关键字) |
| 参数顺序 | 位置 → 默认 → `*args` → 仅关键字 → `**kwargs` |
| 作用域 | LEGB 规则、`global`、`nonlocal` |
| 匿名函数 | `lambda` 语法、使用场景 (sorted/filter/map) |
| 高阶函数 | 函数作为参数/返回值 |
| 递归 | 递归原理、终止条件、递归深度限制 |
| 函数式编程 | `map()`、`filter()`、`reduce()` |

---

### 第 11 章 - 模块与包

**文件：** [11-模块与包.md](./11-模块与包.md)

| 知识点 | 说明 |
|--------|------|
| import 语法 | `import`、`from ... import`、`as` 别名 |
| `__name__` | 直接运行 vs 被导入时的区别 |
| 自定义模块 | 创建 `.py` 模块文件并导入、搜索路径 |
| 包结构 | `__init__.py`、子包、相对导入、`__all__` |
| pip/uv | 安装/卸载/更新/导出依赖 |
| 常用标准库 | math / random / datetime / os / json (各有独立文档) |

> **标准库独立参考文档：**
> - [math.md](./Python 工具箱/标准库/math.md)
> - [random.md](./Python 工具箱/标准库/random.md)
> - [datetime.md](./Python 工具箱/标准库/datetime.md)
> - [os-pathlib.md](./Python 工具箱/标准库/os-pathlib.md)
> - [json.md](./Python 工具箱/标准库/json.md)

---

### 第 12 章 - 文件操作

**文件：** [12-文件操作.md](./12-文件操作.md)

| 知识点 | 说明 |
|--------|------|
| open() 与 with | 文件打开模式 (r/w/a/x/b/t)、上下文管理器 |
| 读取方法 | `read()`、`readline()`、`readlines()`、迭代读取 |
| 写入方法 | `write()`、`writelines()`、追加写入 |
| 文件指针 | `tell()`、`seek()` |
| pathlib | `Path` 对象、路径拼接 (`/`)、`glob`、`rglob` |
| 目录操作 | `mkdir()`、`iterdir()`、`rmdir()`、`shutil` |
| 异常处理 | `FileNotFoundError`、`PermissionError`、编码错误 |
| JSON/CSV 文件 | `json.load/dump`、`csv.reader/writer` |

---

### 第 13 章 - 面向对象编程

**文件：** [13-面向对象编程.md](./13-面向对象编程.md)

| 知识点 | 说明 |
|--------|------|
| 类与对象 | `class` 定义、实例化、`self` |
| `__init__` | 构造方法、实例属性初始化、`__new__` 简介 |
| 实例方法 | 普通方法、`@classmethod`、`@staticmethod` |
| 封装 | 私有属性 (`_`/`__`)、`@property`、setter/deleter |
| 继承 | 单继承、`super()`、方法重写 |
| 多态 | 鸭子类型、运算符重载 (`__str__`/`__repr__`/`__eq__`) |
| 多重继承 | MRO (方法解析顺序)、`C3` 算法、`__mro__` |
| 特殊方法 | `__len__`、`__contains__`、`__iter__`、`__getitem__` |
| 数据类 | `@dataclass`、`field()`、`__post_init__` |

---

### 第 14 章 - 错误与异常

**文件：** [14-错误与异常.md](./14-错误与异常.md)

| 知识点 | 说明 |
|--------|------|
| 异常类型 | `SyntaxError`、`TypeError`、`ValueError`、`AttributeError` 等 |
| try-except | 捕获异常、多个 except 子句、捕获所有异常 |
| else 与 finally | 无异常时执行 (else)、总是执行 (finally) |
| 抛出异常 | `raise`、重新抛出、`raise ... from ...` |
| 自定义异常 | 继承 `Exception`、自定义错误信息、异常链 |
| 上下文管理器 | `with` 语句、`__enter__`/`__exit__`、`contextlib` |
| 断言 | `assert` 语句、调试与生产环境 |
| 日志记录 | `logging` 模块基础、日志级别 |

---

### 第 15 章 - 迭代器与生成器

**文件：** [15-迭代器与生成器.md](./15-迭代器与生成器.md)

| 知识点 | 说明 |
|--------|------|
| 可迭代对象 | `__iter__`、`iter()`、`for` 循环原理 |
| 迭代器协议 | `__next__`、`StopIteration`、自定义迭代器 |
| 生成器函数 | `yield` 关键字、惰性求值、`send()`、`throw()` |
| 生成器表达式 | `(x for x in ...)`、与列表推导式对比 |
| `itertools` | `chain`、`islice`、`product`、`permutations`、`groupby` |
| 实际应用 | 处理大文件、无限序列、管道式数据处理 |

---

### 第 16 章 - 装饰器与闭包

**文件：** [16-装饰器与闭包.md](./16-装饰器与闭包.md)

| 知识点 | 说明 |
|--------|------|
| 闭包 | 自由变量、`nonlocal`、闭包的使用场景 |
| 函数装饰器 | `@decorator` 语法、保留原函数信息 (`@wraps`) |
| 带参数的装饰器 | 三层嵌套结构、工厂函数 |
| 类装饰器 | 用类实现装饰器、`__call__` 方法 |
| 内置装饰器 | `@staticmethod`、`@classmethod`、`@property` |
| 实际应用 | 计时装饰器、缓存 (`@functools.lru_cache`)、权限验证 |
| 装饰器堆叠 | 多个装饰器的执行顺序 |
| `functools` | `wraps`、`partial`、`singledispatch` |

---

## 第三篇：进阶提升篇（第 17-18 章）

### 第 17 章 - 正则表达式

**文件：** [17-正则表达式.md](./17-正则表达式.md)

| 知识点 | 说明 |
|--------|------|
| 元字符 | `.` `^` `$` `*` `+` `?` `{n,m}` `[]` `|` `\` |
| 字符类 | `\d` `\D` `\w` `\W` `\s` `\S`、自定义字符类 |
| 量词 | 贪婪 (`*` `+`) vs 非贪婪 (`*?` `+?`) |
| 分组 | `()` 捕获组、`(?:)` 非捕获组、`(?P<name>)` 命名组 |
| 断言 | `(?=)` 正向前瞻、`(?!)` 负向前瞻、`(?<=)` 后顾 |
| re 模块函数 | `search`、`findall`、`sub`、`split`、`compile`、`match` |
| 标志位 | `re.I` (忽略大小写)、`re.M` (多行)、`re.S` (点匹配换行) |
| 实际应用 | 验证邮箱/手机号、提取 URL、文本清洗、日志分析 |

---

### 第 18 章 - 并发编程

**文件：** [18-并发编程.md](./18-并发编程.md)

| 知识点 | 说明 |
|--------|------|
| 并发 vs 并行 | GIL 的影响、I/O 密集 vs CPU 密集任务 |
| threading | `Thread` 类、`Lock`、`RLock`、`Queue`、线程池 (`ThreadPoolExecutor`) |
| multiprocessing | `Process` 类、`Pool`、进程间通信 (`Queue`/`Pipe`) |
| async/await | 协程基础、`asyncio.run()`、`await` 原理 |
| `asyncio` 基础 | `create_task`、`gather`、`as_completed`、事件循环 |
| 同步原语 | `asyncio.Lock`、`asyncio.Semaphore`、`asyncio.Queue` |
| 选择建议 | 何时用线程、进程、协程、混合使用场景 |

> **asyncio 深入：** [第 23 章 - asyncio 高级编程](./23-asyncio 高级编程.md)

---

## 第四篇：项目实践篇（第 19 章）

### 第 19 章 - 项目实践

**文件：** [19-实用项目.md](./19-实用项目.md)（索引页）
**完整内容：** [19-项目实践/README.md](./19-项目实践/README.md)

| 项目 | 涉及知识点 |
|------|-----------|
| [天气查询工具](./19-项目实践/01-天气查询工具.md) | HTTP 请求 (requests)、JSON 解析、异常处理、API 调用 |
| [文件批量重命名](./19-项目实践/02-文件批量重命名工具.md) | pathlib、文件操作、字符串处理、正则表达式 |
| [待办事项管理器](./19-项目实践/03-待办事项管理器.md) | JSON 持久化、CRUD、面向对象、命令行交互 |
| [网页爬虫](./19-项目实践/04-网页爬虫.md) | BeautifulSoup、HTML 解析、URL 处理、robots.txt |
| [CSV 数据分析](./19-项目实践/05-CSV 数据分析工具.md) | CSV 读写、统计计算、数据可视化、报告生成 |

---

## 第五篇：Python 工具箱

### Python 工具箱总览

**目录：** [Python 工具箱/](./Python 工具箱/README.md)

整合第 11、12、17 章完整内容，并将第 11 章的标准库拆分为独立参考文档。

```
Python 工具箱/
├── README.md              # 工具箱导航
├── 11-模块与包.md         # 完整内容
├── 12-文件操作.md         # 完整内容
├── 17-正则表达式.md       # 完整内容
├── 常用第三方库/          # (待补充)
│   ├── requests.md
│   ├── beautifulsoup4.md
│   └── ...
└── 标准库/
    ├── math.md           # 数学函数
    ├── random.md         # 随机数生成
    ├── datetime.md       # 日期时间处理
    ├── os-pathlib.md     # 文件系统操作
    ├── json.md           # JSON 数据解析
    ├── collections.md    # (待补充)
    └── itertools.md      # (待补充)
```

---

## 第六篇：专题深入篇（第 20-23 章）

### 第 20 章 - uv 包管理器

**文件：** [20-uv 包管理器.md](./20-uv 包管理器.md)

| 知识点 | 说明 |
|--------|------|
| uv 简介 | 为什么选择 uv、与 pip/poetry 的对比 |
| uv 安装 | 一键安装脚本、版本管理 |
| 虚拟环境 | `uv venv`、激活与停用、`uv run` |
| 依赖管理 | `uv add`、`uv remove`、`uv sync`、锁定文件 |
| Python 版本管理 | `uv python install`、多版本切换 |
| `pyproject.toml` | 现代项目配置格式、脚本定义 |
| 发布包 | `uv build`、`uv publish` |

---

### 第 21 章 - 类型提示

**文件：** [21-类型提示.md](./21-类型提示.md)

| 知识点 | 说明 |
|--------|------|
| 基础类型注解 | `int`、`str`、`list`、`dict`、`tuple`、`set` |
| 可选类型 | `Optional[X]` (3.9 前)、`X | None` (3.10+) |
| 联合类型 | `Union[X, Y]` (旧)、`X | Y` (新语法) |
| 泛型 | `List[T]`、`Dict[K, V]` (旧) vs `list[T]` (新，3.9+) |
| 函数注解 | 参数类型、返回值类型、`-> None` |
| 类型别名 | `type` 关键字 (3.12+)、`TypeAlias` |
| `TypeVar` 与协议 | `Protocol`、结构子类型、泛型函数 |
| `typing` 模块 | `Any`、`Literal`、`TypedDict`、`NamedTuple` |
| mypy | 静态类型检查工具、配置与使用 |

---

### 第 22 章 - 列表推导式

**文件：** [22-列表推导式.md](./22-列表推导式.md)

| 知识点 | 说明 |
|--------|------|
| 列表推导式 | `[expr for x in iter if cond]`、嵌套推导式 |
| 字典推导式 | `{k: v for k, v in ...}`、条件过滤 |
| 集合推导式 | `{expr for x in iter}`、与列表推导式对比 |
| 生成器表达式 | `(expr for x in iter)`、惰性求值、内存优势 |
| 嵌套推导式 | 多层 for 的展开、矩阵操作 |
| 性能对比 | 推导式 vs 循环 vs map/filter、时间复杂度分析 |
| 最佳实践 | 可读性优先、避免过度嵌套、适当拆分 |

---

### 第 23 章 - asyncio 高级编程

**文件：** [23-asyncio 高级编程.md](./23-asyncio 高级编程.md)

| 知识点 | 说明 |
|--------|------|
| 事件循环 | `asyncio.get_event_loop()`、事件循环工作原理、多循环场景 |
| Task 与 Future | `asyncio.create_task()`、`await` 一个 Future、`asyncio.ensure_future()` |
| `asyncio.Queue` | 生产者 - 消费者模式、`put()`/`get()`、`join()` |
| 同步原语 | `Lock`、`Event`、`Semaphore`、`Condition` |
| 超时控制 | `asyncio.wait_for()`、`asyncio.timeout()`、`asyncio.shield()` |
| 并发运行 | `asyncio.gather()`、`asyncio.wait()`、`asyncio.as_completed()` |
| 与同步代码结合 | `run_in_executor()`、混用同步库、线程池/进程池 |
| 调试技巧 | `PYTHONASYNCIODEBUG`、`loop.set_debug()` |

---

## 第七篇：服务端编程篇（第 24-29 章）⭐ 新增

```
┌─────────────────────────────────────────────────────────────────┐
│              第七篇：服务端编程篇 学习路线                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  基础理论 → Flask 框架 (同步) → FastAPI 框架 (异步) → 实战项目   │
│                                                                 │
│  第 24 章      第 25-27 章       第 28 章        第 29 章         │
│  Web 基础     Flask 全三章     FastAPI 详解    3 个完整项目      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 第 24 章 - Web 基础

**文件：** [24-Web 基础.md](./07-服务端编程篇/24-Web 基础.md)

| 知识点 | 说明 |
|--------|------|
| HTTP 协议详解 | HTTP/1.1 vs HTTP/2、请求/响应结构、无状态特性 |
| HTTP 方法 | GET (查询)、POST (创建)、PUT (更新)、DELETE (删除)、PATCH (部分更新) |
| HTTP 状态码 | 1xx (信息)、2xx (成功)、3xx (重定向)、4xx (客户端错误)、5xx (服务器错误) |
| 请求头与响应头 | Content-Type、Accept、Authorization、Cookie、User-Agent |
| URL 结构解析 | scheme、host、port、path、query、fragment |
| 路径参数与查询参数 | `/users/{id}` vs `/users?id=123` |
| RESTful 路由设计 | 资源命名、复数形式、层级资源、过滤/排序/分页 |
| Cookie 原理 | Cookie 结构、Secure/HttpOnly/SameSite 属性 |
| Session 机制 | 服务端 Session、Session ID、存储方式 |
| Token 认证 | JWT 结构 (Header/Payload/Signature)、Bearer Token |
| RESTful API 设计 | 资源命名规范、版本控制 (`/api/v1/`)、错误处理规范 |
| 文档化 | OpenAPI/Swagger 规范、API 文档工具 |

---

### 第 25 章 - Flask 快速上手

**文件：** [25-Flask 快速上手.md](./07-服务端编程篇/25-Flask 快速上手.md)

| 知识点 | 说明 |
|--------|------|
| Flask 简介 | 微框架理念、轻量灵活、扩展生态 |
| 安装与配置 | `uv add flask`、环境变量、配置文件 |
| 第一个 Flask 应用 | 最小应用结构、`app.run()`、开发服务器 |
| 调试模式 | `FLASK_DEBUG=1`、`app.debug = True`、自动重载 |
| 路由基础 | `@app.route` 装饰器、URL 规则 |
| 动态路由 | `<int:id>`、`<string:name>`、自定义转换器 |
| URL 构建 | `url_for()`、端点命名、静态文件 |
| 重定向与 abort | `redirect()`、`abort(404)`、错误处理 |
| request 对象 | 请求上下文、`request.method`、`request.args` |
| 表单数据处理 | `request.form`、`request.get_json()` |
| 文件上传 | `request.files`、`save()`、文件验证 |
| 请求钩子 | `before_request`、`after_request`、`teardown_request` |
| Response 对象 | `Response()`、`jsonify()`、自定义响应头 |
| 会话管理 | `session` 对象、`SECRET_KEY`、加密 Cookie |

---

### 第 26 章 - Flask 进阶

**文件：** [26-Flask 进阶.md](./07-服务端编程篇/26-Flask 进阶.md)

| 知识点 | 说明 |
|--------|------|
| **模板引擎 (Jinja2)** | |
| 模板渲染 | `render_template()`、模板目录结构 |
| 变量与表达式 | `{{ variable }}`、过滤器 (`|upper`)、默认值 |
| 控制结构 | `{% if %}`、`{% for %}`、`{% else %}` |
| 模板继承 | `{% extends %}`、`{% block %}`、`{{ super() }}` |
| 宏 (macro) | `{% macro %}`、`{% call %}`、参数传递 |
| 自定义过滤器 | `@app.template_filter`、全局可用 |
| **蓝图 (Blueprint)** | |
| 为什么需要蓝图 | 模块化、大型应用组织、代码分离 |
| 创建与注册蓝图 | `Blueprint('name', __name__)`、`app.register_blueprint()` |
| 蓝图 URL 前缀 | `url_prefix` 参数、命名空间隔离 |
| 应用工厂模式 | `create_app()` 函数、多实例配置、测试友好 |
| **数据库集成** | |
| Flask-SQLAlchemy | 安装配置、`SQLALCHEMY_DATABASE_URI` |
| 模型定义 | `db.Model`、列定义、主键、外键 |
| CRUD 操作 | `db.session.add()`、`filter()`、`all()`、`first()` |
| 关系定义 | 一对多 (`ForeignKey`)、多对多 (`secondary`) |
| 数据库迁移 | Flask-Migrate、`flask db init/migrate/upgrade` |
| **用户认证** | |
| Flask-Login | `LoginManager`、用户加载回调 |
| UserMixin | 预置的用户类混合、4 个必需属性 |
| 登录/登出 | `login_user()`、`logout_user()` |
| 登录保护 | `@login_required`、未登录跳转 |

---

### 第 27 章 - Flask 高级主题

**文件：** [27-Flask 高级.md](./07-服务端编程篇/27-Flask 高级.md)

| 知识点 | 说明 |
|--------|------|
| **RESTful API 开发** | |
| Flask-RESTful / Flask-Smorest | API 专用扩展、资源类、请求解析 |
| 资源类 (Resource) | `get()`、`post()`、`put()`、`delete()` 方法 |
| 请求解析 (reqparse) | `Argument`、类型验证、必需参数、错误消息 |
| 字段序列化 | `fields`、格式化、嵌套字段 |
| API 版本控制 | URL 版本 (`/api/v1/`)、请求头版本、蓝图隔离 |
| **认证与授权** | |
| HTTP Basic Auth | `Authorization` 头、`username:password` Base64 |
| JWT Token 认证 | `PyJWT`、Token 生成、Token 验证、过期处理 |
| OAuth2 登录 | Flask-Dance、第三方登录 (GitHub/Google) |
| 权限控制 | 基于角色的访问控制 (RBAC)、装饰器实现 |
| **文件处理** | |
| 文件上传 | `SecureFilename`、文件类型验证、大小限制 |
| 文件存储 | 本地存储、云存储 (S3/OSS) 集成 |
| 文件下载 | `send_file()`、`send_from_directory()` |
| **缓存** | |
| Flask-Caching | 配置缓存后端、`@cache.cached` 装饰器 |
| Redis 缓存 | 连接配置、缓存失效、缓存预热 |
| 视图缓存 | 整页缓存、片段缓存、缓存键设计 |
| **异步任务** | |
| Celery 集成 | Celery 配置、任务定义、Broker (Redis/RabbitMQ) |
| 任务队列 | 延迟执行、重试机制、任务监控 |
| 定时任务 | Celery Beat、Cron 表达式 |
| **测试与部署** | |
| Flask 测试客户端 | `app.test_client()`、模拟请求、断言响应 |
| pytest-flask | fixture、数据库隔离、测试覆盖率 |
| Gunicorn + Nginx | WSGI 服务器、反向代理、负载均衡 |
| Docker 容器化 | Dockerfile 编写、多阶段构建、docker-compose |

---

### 第 28 章 - FastAPI 详解

**文件：** [28-FastAPI.md](./07-服务端编程篇/28-FastAPI.md)

| 知识点 | 说明 |
|--------|------|
| **FastAPI 简介** | |
| 为什么选择 FastAPI | 高性能 (Starlette + Pydantic)、自动文档、类型检查 |
| 安装与运行 | `uv add fastapi uvicorn`、`uvicorn main:app --reload` |
| ASGI 与异步 | ASGI 协议、异步应用、与 WSGI 对比 |
| **路径操作** | |
| 路径装饰器 | `@app.get()`、`@app.post()`、`@app.put()`、`@app.delete()` |
| 路径参数 | `{item_id}`、类型转换、`Path()` 验证 |
| 查询参数 | 函数参数、默认值、`Query()` 验证 |
| 请求体 | Pydantic 模型、嵌套模型、可选字段 |
| **数据验证 (Pydantic)** | |
| 请求体模型 | `BaseModel`、字段定义、类型注解 |
| 字段验证器 | `Field()`、`gt`/`lt`/`min_length`、正则匹配 |
| 嵌套模型 | 多层嵌套、列表字段、字典字段 |
| 响应模型 | `response_model`、排除字段、序列化 |
| **依赖注入系统** | |
| Depends 使用 | `Depends()`、子依赖、可组合 |
| 依赖类 | 类作为依赖、`__call__` 方法 |
| 全局依赖 | `app.dependencies`、路由前缀依赖 |
| 依赖覆盖 | 测试环境依赖替换 |
| **数据库集成** | |
| SQLAlchemy 异步 | `AsyncSession`、`async def` 会话管理 |
| 数据库会话管理 | 依赖注入会话、会话关闭、异常回滚 |
| CRUD 封装 | 基类封装、通用查询方法 |
| 异步 ORM | `await session.execute()`、结果映射 |
| **认证与安全** | |
| OAuth2 Password Flow | `OAuth2PasswordBearer`、Token 端点 |
| JWT Token | `python-jose`、Token 创建、Token 解析 |
| 密码哈希 | `bcrypt`、`pwd_context.hash()`、`verify()` |
| 安全工具 | `HTTPBearer`、`Security()`、 scopes |
| **自动文档** | |
| Swagger UI | `/docs` 交互式文档、Try it out |
| ReDoc | `/redoc` 美观文档、只读模式 |
| 自定义文档配置 | `title`、`version`、`description`、Tags |
| **WebSocket 支持** | |
| WebSocket 端点 | `@app.websocket()`、连接接受 |
| 消息收发 | `receive_text()`、`send_text()`、JSON 消息 |
| 连接管理 | 连接池、广播、房间管理 |
| **后台任务** | |
| BackgroundTasks | 请求后执行、邮件发送、日志记录 |
| **测试与部署** | |
| httpx 测试客户端 | `AsyncClient`、异步测试、模拟 WebSocket |
| pytest-asyncio | 异步 fixture、事件循环管理 |
| Uvicorn + Gunicorn | 生产部署、worker 配置、进程管理 |

---

### 第 29 章 - 服务端项目实战

**文件：** [29-服务端项目实战.md](./07-服务端编程篇/29-服务端项目实战.md)

| 项目 | 技术栈 | 功能模块 |
|------|--------|---------|
| **项目一：博客 API**<br>(Flask) | Flask<br>SQLAlchemy<br>JWT<br>Flask-Migrate | • 用户注册/登录 (JWT 认证)<br>• 文章 CRUD (富文本存储)<br>• 评论系统 (嵌套评论)<br>• 标签与分类 (多对多关系)<br>• 用户个人信息管理 |
| **项目二：待办事项 API**<br>(FastAPI) | FastAPI<br>Async SQLAlchemy<br>Pydantic<br>alembic | • 异步数据库连接<br>• JWT 认证与授权<br>• 完整的 CRUD 操作<br>• 任务状态流转<br>• 分类与标签<br>• 完整的测试用例 |
| **项目三：实时聊天室**<br>(FastAPI WebSocket) | FastAPI<br>WebSocket<br>Redis | • WebSocket 连接管理<br>• 多房间系统<br>• 消息广播<br>• 在线用户显示<br>• 消息持久化<br>• 用户认证 (Token) |

---

## 第八篇：机器学习篇（第 30-34 章）⭐ 新增

```
┌─────────────────────────────────────────────────────────────────┐
│                  第八篇：机器学习篇 学习路线                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  理论基础 → 数据预处理 → 监督学习 → 无监督学习 → 模型评估优化    │
│    ↓            ↓            ↓            ↓            ↓        │
│   第 30 章      第 31 章       第 32 章       第 33 章       第 34 章  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 第 30 章 - 机器学习基础

**文件：** [30-机器学习基础.md](./08-机器学习篇/30-机器学习基础.md)

| 知识点 | 说明 |
|--------|------|
| 什么是机器学习 | 定义、与传统编程的区别、应用场景 |
| 机器学习工作流程 | 数据收集→数据清洗→特征工程→模型训练→评估→部署 |
| 学习类型概览 | 监督学习、无监督学习、半监督学习、强化学习 |
| 监督学习 vs 无监督学习 | 标签数据 vs 无标签数据、分类/回归 vs 聚类/降维 |
| scikit-learn 简介 | 安装 (`uv add scikit-learn`)、API 设计哲学、核心组件 |
| 数据集格式 | 特征矩阵 X、目标向量 y、训练集/测试集 |
| 模型训练流程 | `fit()`、`predict()`、`score()` |
| 模型选择 | 算法选择指南、数据量与特征数考虑 |
| 交叉验证 | k 折交叉验证、`cross_val_score()` |
| 过拟合与欠拟合 | 识别方法、解决策略 (正则化、特征选择) |

---

### 第 31 章 - 数据预处理

**文件：** [31-数据预处理.md](./08-机器学习篇/31-数据预处理.md)

| 知识点 | 说明 |
|--------|------|
| **数据清洗** | |
| 缺失值处理 | 删除缺失值、均值/中位数/众数填充、插值法 |
| 异常值检测 | 3σ原则、IQR 方法、箱线图识别 |
| 重复值处理 | `drop_duplicates()`、业务规则去重 |
| **特征工程** | |
| 类别特征编码 | 独热编码 (`OneHotEncoder`)、标签编码 (`LabelEncoder`) |
| 数值特征转换 | 对数转换、Box-Cox 转换、分箱离散化 |
| 特征缩放 | 标准化 (`StandardScaler`)、归一化 (`MinMaxScaler`) |
| 特征选择 | 方差选择、相关系数、递归特征消除 (RFE) |
| 特征构造 | 多项式特征、交互特征、领域知识特征 |
| **数据集划分** | |
| 训练集/测试集划分 | `train_test_split()`、分层抽样、随机种子 |
| 验证集 | 三折划分、交叉验证替代 |
| 时间序列划分 | 时序数据不能随机打乱、滚动窗口验证 |
| **数据不平衡处理** | |
| 问题识别 | 类别分布分析、混淆矩阵 |
| 欠采样 | 随机欠采样、 Tomek Links |
| 过采样 | 随机过采样、SMOTE 合成 |
| 类别权重 | `class_weight='balanced'` |

---

### 第 32 章 - 经典算法（上）- 监督学习

**文件：** [32-经典算法 - 监督学习.md](./08-机器学习篇/32-经典算法 - 监督学习.md)

| 知识点 | 说明 |
|--------|------|
| **线性回归** | |
| 原理 | 最小二乘法、损失函数、梯度下降 |
| sklearn 实现 | `LinearRegression`、` Ridge` (L2 正则)、`Lasso` (L1 正则) |
| 评估指标 | R²分数、MSE、RMSE、MAE |
| 多元回归 | 多特征、共线性问题、VIF |
| **逻辑回归** | |
| 原理 | Sigmoid 函数、对数几率、最大似然估计 |
| sklearn 实现 | `LogisticRegression`、正则化参数 C |
| 多分类 | OvR (One vs Rest)、OvO (One vs One)、Softmax |
| 评估指标 | 准确率、精确率、召回率、F1、ROC-AUC |
| **决策树** | |
| 原理 | 信息增益/基尼系数、递归分裂、剪枝策略 |
| sklearn 实现 | `DecisionTreeClassifier`、`DecisionTreeRegressor` |
| 超参数 | `max_depth`、`min_samples_split`、`max_features` |
| 可视化 | `plot_tree()`、特征重要性 |
| **随机森林** | |
| 原理 | Bagging 集成、多棵树投票/平均 |
| sklearn 实现 | `RandomForestClassifier`、`RandomForestRegressor` |
| 超参数 | `n_estimators`、`max_features`、`bootstrap` |
| 特征重要性 | 平均不纯度减少、排列重要性 |
| **支持向量机 (SVM)** | |
| 原理 | 最大间隔超平面、支持向量、软间隔 |
| 核函数 | 线性核、多项式核、RBF 核、Sigmoid 核 |
| sklearn 实现 | `SVC`、`SVR`、`LinearSVC` |
| 超参数 | C (正则化)、gamma (核系数) |
| 适用场景 | 小样本、高维度、非线性问题 |

---

### 第 33 章 - 经典算法（下）- 无监督学习

**文件：** [33-经典算法 - 无监督学习.md](./08-机器学习篇/33-经典算法 - 无监督学习.md)

| 知识点 | 说明 |
|--------|------|
| **K-Means 聚类** | |
| 原理 | 质心初始化、迭代分配、质心更新 |
| sklearn 实现 | `KMeans`、`fit_predict()` |
| K 值选择 | 手肘法、轮廓系数、业务解释 |
| 优缺点 | 简单高效 vs 需指定 K、对异常值敏感 |
| 应用场景 | 客户分群、图像压缩、文档聚类 |
| **层次聚类** | |
| 原理 | 凝聚式 (自底向上)、分裂式 (自顶向下) |
| 距离度量 | 欧氏距离、曼哈顿距离、余弦相似度 |
| 链接方式 | 单链接、全链接、平均链接、ward 法 |
| sklearn 实现 | `AgglomerativeClustering` |
| 树状图 | `scipy.cluster.hierarchy.dendrogram` |
| **DBSCAN 密度聚类** | |
| 原理 | 核心点、边界点、噪声点、密度可达 |
| sklearn 实现 | `DBSCAN`、`eps`、`min_samples` |
| 优缺点 | 任意形状聚类 vs 参数敏感、密度差异大时效果差 |
| **主成分分析 (PCA)** | |
| 原理 | 最大方差方向、特征值分解、降维 |
| sklearn 实现 | `PCA`、`fit_transform()`、`explained_variance_ratio_` |
| 主成分选择 | 累计解释方差比、碎石图 |
| 应用场景 | 数据可视化、去噪、特征降维 |
| **关联规则** | |
| 概念 | 支持度、置信度、提升度 |
| Apriori 算法 | 频繁项集生成、规则提取 |
| 应用场景 | 购物篮分析、推荐系统 |

---

### 第 34 章 - 模型评估与优化

**文件：** [34-模型评估与优化.md](./08-机器学习篇/34-模型评估与优化.md)

| 知识点 | 说明 |
|--------|------|
| **分类评估指标** | |
| 混淆矩阵 | TP、TN、FP、FN、`confusion_matrix()` |
| 准确率 | (TP+TN)/(TP+TN+FP+FN)、不适用于不平衡数据 |
| 精确率 | TP/(TP+FP)、查准率 |
| 召回率 | TP/(TP+FN)、查全率、灵敏度 |
| F1 分数 | 2×精确率×召回率/(精确率 + 召回率)、调和平均 |
| ROC 曲线 | FPR vs TPR、`roc_curve()` |
| AUC | ROC 曲线下面积、模型整体性能 |
| **回归评估指标** | |
| MSE | 均方误差、对异常值敏感 |
| RMSE | 均方根误差、与目标变量同量纲 |
| MAE | 平均绝对误差、鲁棒性更好 |
| R²分数 | 决定系数、拟合优度 |
| **交叉验证** | |
| k 折交叉验证 | `KFold`、`cross_val_score()`、k=5 或 10 |
| 分层交叉验证 | `StratifiedKFold`、保持类别比例 |
| 留一法 | LOOCV、计算代价高、无偏估计 |
| 时间序列交叉验证 | `TimeSeriesSplit`、滚动预测 |
| **超参数调优** | |
| 网格搜索 | `GridSearchCV`、穷举搜索、计算量大 |
| 随机搜索 | `RandomizedSearchCV`、采样搜索、效率高 |
| 贝叶斯优化 | `BayesianOptimization`、智能搜索 |
| 学习曲线 | 诊断过拟合/欠拟合、决定是否需要更多数据 |
| 验证曲线 | 超参数 vs 性能、选择最佳参数范围 |
| **模型保存与加载** | |
| pickle | `pickle.dump()`、`pickle.load()` |
| joblib | `joblib.dump()`、`joblib.load()`、更高效 |
| ONNX 格式 | 跨平台模型交换、部署优化 |

---

## 第九篇：神经网络与深度学习篇（第 35-39 章）⭐ 新增

```
┌─────────────────────────────────────────────────────────────────┐
│             第九篇：神经网络与深度学习篇 学习路线                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  神经网络基础 → PyTorch 入门 → CNN → RNN → 实战项目              │
│      ↓            ↓          ↓       ↓        ↓                 │
│    第 35 章       第 36 章     第 37 章   第 38 章    第 39 章      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 第 35 章 - 神经网络基础

**文件：** [35-神经网络基础.md](./09-神经网络与深度学习篇/35-神经网络基础.md)

| 知识点 | 说明 |
|--------|------|
| **神经元与感知机** | |
| 生物神经元 | 树突、轴突、突触、电化学信号 |
| 人工神经元 | 输入、权重、偏置、激活函数、输出 |
| 感知机 | 单层感知机、线性可分、XOR 问题 |
| **多层感知机 (MLP)** | |
| 网络结构 | 输入层、隐藏层、输出层、全连接 |
| 前向传播 | 逐层计算、从输入到输出 |
| 参数规模 | 权重矩阵、偏置向量、计算量 |
| **激活函数** | |
| Sigmoid | 公式、图像、梯度消失问题、二分类输出 |
| Tanh | 双曲正切、零中心化、仍会梯度消失 |
| ReLU | 线性整流、解决梯度消失、稀疏激活 |
| Leaky ReLU | 允许负值、解决 Dead ReLU |
| Softmax | 多分类输出、概率分布 |
| **反向传播与梯度下降** | |
| 损失函数 | 均方误差 (回归)、交叉熵 (分类) |
| 梯度计算 | 链式法则、自动微分 |
| 梯度下降 | Batch GD、Stochastic GD、Mini-batch GD |
| 优化器 | SGD + Momentum、Adam、RMSprop |
| 学习率 | 学习率调度、学习率衰减 |
| **训练技巧** | |
| 权重初始化 | Xavier 初始化、He 初始化 |
| 批量归一化 | BatchNorm、加速收敛、允许更大学习率 |
| Dropout | 随机失活、防止过拟合 |
| 早停法 | 验证集监控、避免过拟合 |

---

### 第 36 章 - PyTorch 入门

**文件：** [36-PyTorch 入门.md](./09-神经网络与深度学习篇/36-PyTorch 入门.md)

| 知识点 | 说明 |
|--------|------|
| **PyTorch 简介** | |
| 为什么选择 PyTorch | 动态计算图、Pythonic、研究友好、生态丰富 |
| 安装 | `uv add torch torchvision`、GPU 支持 (CUDA) |
| **Tensor 基础** | |
| Tensor 创建 | `torch.tensor()`、`torch.zeros()`、`torch.randn()` |
| Tensor 属性 | `shape`、`dtype`、`device`、`requires_grad` |
| Tensor 操作 | 索引切片、reshape、view、transpose |
| 运算 | 加减乘除、矩阵乘法 (`matmul`)、广播机制 |
| GPU 加速 | `.to('cuda')`、`.cuda()`、设备无关代码 |
| **自动求导 (autograd)** | |
| 计算图 | 动态构建、反向传播 |
| `requires_grad` | 追踪梯度、叶子节点 |
| `backward()` | 反向传播、累积梯度 |
| `grad` 属性 | 梯度值、梯度清零 (`optimizer.zero_grad()`) |
| **nn.Module 与网络搭建** | |
| 模块类 | 继承 `nn.Module`、`__init__` 定义层、`forward` 定义前向 |
| 常用层 | `nn.Linear`、`nn.Conv2d`、`nn.LSTM`、`nn.Dropout` |
| 顺序模型 | `nn.Sequential`、简洁定义 |
| 模型保存与加载 | `state_dict`、`torch.save()`、`torch.load()` |
| **数据加载 (DataLoader)** | |
| Dataset | 自定义 Dataset、`__len__`、`__getitem__` |
| DataLoader | `batch_size`、`shuffle`、`num_workers` |
| torchvision.datasets | MNIST、CIFAR-10、ImageFolder |
| 数据增强 | `transforms`、随机裁剪、翻转、归一化 |
| **训练流程** | |
| 损失函数 | `nn.MSELoss`、`nn.CrossEntropyLoss` |
| 优化器 | `torch.optim.SGD`、`torch.optim.Adam` |
| 训练循环 | 前向→损失→反向→更新、梯度清零 |
| 评估模式 | `model.eval()`、`torch.no_grad()` |

---

### 第 37 章 - 卷积神经网络 (CNN)

**文件：** [37-卷积神经网络 CNN.md](./09-神经网络与深度学习篇/37-卷积神经网络 CNN.md)

| 知识点 | 说明 |
|--------|------|
| **卷积层** | |
| 卷积操作 | 卷积核、步长、填充、特征图 |
| 2D 卷积 | `nn.Conv2d`、多通道、多卷积核 |
| 1x1 卷积 | 通道混合、降维/升维 |
| **池化层** | |
| 最大池化 | `nn.MaxPool2d`、保留显著特征 |
| 平均池化 | `nn.AvgPool2d`、平滑特征 |
| 全局池化 | 替代全连接层、减少参数 |
| **经典架构** | |
| LeNet-5 | 首个成功 CNN、手写数字识别 |
| AlexNet | ReLU、Dropout、ImageNet 突破 |
| VGG | 小卷积核堆叠、深度网络 |
| ResNet | 残差连接、解决退化问题、极深网络 |
| **实战：图像分类** | |
| 数据准备 | CIFAR-10/CIFAR-100、数据增强 |
| 模型搭建 | 自定义 CNN、迁移学习 (ResNet 预训练) |
| 训练策略 | 学习率调度、早停、模型检查点 |
| 评估 | 准确率、混淆矩阵、错误分析 |
| **进阶主题** | |
| 迁移学习 | 冻结骨干网络、微调顶层、特征提取器 |
| 数据增强 | `torchvision.transforms`、AutoAugment |
| 可视化 | 特征图可视化、Grad-CAM、注意力热力图 |

---

### 第 38 章 - 循环神经网络 (RNN)

**文件：** [38-循环神经网络 RNN.md](./09-神经网络与深度学习篇/38-循环神经网络 RNN.md)

| 知识点 | 说明 |
|--------|------|
| **RNN 基础** | |
| 序列建模 | 时间序列、文本序列、语音信号 |
| RNN 结构 | 隐藏状态、循环连接、参数共享 |
| 前向传播 | 逐时间步展开、隐藏状态传递 |
| 梯度问题 | 梯度消失/爆炸、长序列训练困难 |
| **LSTM** | |
| 门控机制 | 遗忘门、输入门、输出门 |
| 细胞状态 | 长期记忆、梯度流动 |
| PyTorch 实现 | `nn.LSTM`、`num_layers`、`bidirectional` |
| **GRU** | |
| 简化 LSTM | 更新门、重置门、参数更少 |
| PyTorch 实现 | `nn.GRU`、与 LSTM 对比 |
| **序列建模任务** | |
| 一对多 | 图像描述生成 |
| 多对一 | 情感分类、序列分类 |
| 多对多 | 机器翻译、序列标注 |
| **实战：文本分类/情感分析** | |
| 文本预处理 | 分词、构建词汇表、文本编码 |
| 词嵌入 | `nn.Embedding`、预训练词向量 (Word2Vec/GloVe) |
| 模型搭建 | LSTM/GRU 分类器、注意力机制简介 |
| 训练与评估 | 准确率、F1、混淆矩阵 |

---

### 第 39 章 - 深度学习实战项目

**文件：** [39-深度学习实战.md](./09-神经网络与深度学习篇/39-深度学习实战.md)

| 项目 | 技术栈 | 功能模块 |
|------|--------|---------|
| **项目一：图像分类**<br>猫狗识别 | CNN<br>ResNet(迁移学习)<br>torchvision | • 数据集准备与探索<br>• 数据增强 pipeline<br>• 从零训练 CNN<br>• 使用 ResNet 预训练模型<br>• 迁移学习与微调<br>• 模型对比与结果分析 |
| **项目二：文本生成**<br>唐诗生成 | LSTM/GRU<br>词嵌入<br>语言模型 | • 唐诗数据集预处理<br>• 字符级/词级建模<br>• LSTM 语言模型<br>• 温度采样策略<br>• 生成质量评估 |
| **项目三：迁移学习实战**<br>使用预训练模型 | ResNet/VGG<br>特征提取<br>微调 | • 使用 ImageNet 预训练权重<br>• 冻结骨干网络<br>• 替换分类头<br>• 两阶段训练 (冻结→微调)<br>• 小数据集场景最佳实践 |

---

## 学习路径建议（按方向）

```
┌─────────────────────────────────────────────────────────────────┐
│                    学习路线选择                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Python 后端开发                                                 │
│  ①→②→③→④→⑤→⑥→⑦ (Flask/FastAPI 全栈)                         │
│                                                                 │
│  Web 全栈开发                                                    │
│  ①→②→③→④→⑦ + (HTML/CSS/JS + Vue/React)                        │
│                                                                 │
│  机器学习/数据分析                                               │
│  ①→②→④→⑤→⑧ (scikit-learn + pandas/numpy)                      │
│                                                                 │
│  深度学习/计算机视觉                                             │
│  ①→②→⑧→⑨ (PyTorch + CNN + 项目实战)                            │
│                                                                 │
│  AI 应用开发 (LLM 方向)                                          │
│  ①→②→⑥(asyncio)→⑦(FastAPI)→⑨(基础) + LangChain               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

| 方向 | 推荐篇章 | 目标岗位 |
|------|---------|---------|
| **Python 开发** | 1→2→3→4→5→6 | Python 后端/自动化 |
| **Web 后端开发** | 1→2→3→4→7 | Flask/Django/FastAPI 工程师 |
| **全栈开发** | 1→2→3→4→7+ 前端 | 全栈工程师 |
| **机器学习** | 1→2→5→8 | ML 工程师/算法 |
| **深度学习** | 1→2→5→8→9 | CV/NLP 算法工程师 |
| **AI 应用开发** | 1→2→7→8→9 | AI 应用工程师 |

---

## 学习方法建议

```
┌─────────────────────────────────────────┐
│           学习方法四原则                │
├─────────────────────────────────────────┤
│                                         │
│  1. 循序渐进                            │
│     按篇章顺序学习，不要跳章            │
│     基础篇是进阶篇的前提               │
│                                         │
│  2. 动手实践                            │
│     每章代码示例务必亲手运行            │
│     修改参数，观察输出变化              │
│                                         │
│  3. 完成练习                            │
│     每章末尾有练习题，先自己做          │
│     做不出来再看答案                    │
│                                         │
│  4. 遇到问题先查本教程                  │
│     本教程包含大量常见错误说明          │
│     每章"常见错误"部分重点阅读         │
│                                         │
└─────────────────────────────────────────┘
```

---

## 环境要求

| 工具 | 版本 | 说明 |
|------|------|------|
| Python | 3.11+ | 推荐通过 uv 安装 |
| uv | 最新版 | 包管理器 (替代 pip + venv) |
| 编辑器 | VS Code | 安装 Python 插件 |

安装 uv（macOS/Linux）：
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

安装 Python 并创建项目：
```bash
uv python install 3.11
uv init my-python-project
cd my-python-project
uv run python main.py
```

---

[进入第 1 章](./01-Python 简介与环境搭建.md)
