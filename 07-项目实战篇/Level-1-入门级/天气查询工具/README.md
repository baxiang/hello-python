# 天气查询工具 (Level 1)

简单的命令行天气查询脚本。

## 目标
- 练习 `requests` 库的使用。
- 解析 JSON 数据。
- 理解 API 调用的基本流程。

## 运行

1. **修改 API Key**:
   打开 `weather_query.py`，将 `YOUR_API_KEY` 替换为真实的和风天气 API Key。

2. **安装依赖**:
   ```bash
   uv sync
   ```

3. **运行脚本**:
   ```bash
   uv run python weather_query.py
   ```
