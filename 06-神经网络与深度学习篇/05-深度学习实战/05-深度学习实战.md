# 第 39 章 - 深度学习实战（详细版）

> **Python 版本要求：** 本章代码需要 Python 3.11+ 运行环境

本章通过 3 个完整的深度学习项目，综合运用 CNN、RNN 和迁移学习技术。

---

## 实际场景

**迁移学习：快速构建图像分类模型**

假设你需要为公司开发一个产品图片分类系统（识别不同类别的商品）。从头训练 CNN 需要大量数据和计算资源。使用迁移学习，你可以：

1. 加载预训练的 ResNet 模型（已在百万张图片上训练）
2. 冻结骨干网络参数（保留已学到的特征提取能力）
3. 只训练最后的分类层（适配你的产品类别）
4. 几小时就能达到良好效果，无需大量数据

迁移学习是深度学习的核心技术，让你快速构建高性能模型。

---

## 项目一：图像分类 - 猫狗识别

### 39.1 项目概述

使用 CNN 和迁移学习实现猫狗图像分类，这是一个经典的二分类问题。

**项目目标：**
- 掌握图像分类完整流程
- 学习数据增强技术
- 实践迁移学习方法
- 对比从零训练 vs 微调预训练模型

**数据集：**
- Kaggle Dogs vs Cats 数据集
- 25,000 张训练图像（猫 12,500，狗 12,500）
- 图像尺寸不一，需统一处理

---

### 39.2 从零训练 CNN

```python
import torch
import torch.nn as nn
import torch.optim as optim
from torchvision import datasets, transforms, models
from torch.utils.data import DataLoader
import matplotlib.pyplot as plt

# 1. 数据准备
data_dir = 'data/dogs_vs_cats'

# 数据增强（训练集）
train_transforms = transforms.Compose([
    transforms.RandomResizedCrop(224),      # 随机裁剪 + 缩放
    transforms.RandomHorizontalFlip(),       # 随机水平翻转
    transforms.RandomRotation(15),           # 随机旋转
    transforms.ColorJitter(brightness=0.2,   # 颜色抖动
                          contrast=0.2,
                          saturation=0.2),
    transforms.ToTensor(),
    transforms.Normalize([0.485, 0.456, 0.406],
                        [0.229, 0.224, 0.225])
])

# 仅归一化（验证集）
val_transforms = transforms.Compose([
    transforms.Resize(256),
    transforms.CenterCrop(224),
    transforms.ToTensor(),
    transforms.Normalize([0.485, 0.456, 0.406],
                        [0.229, 0.224, 0.225])
])

# 加载数据
train_dataset = datasets.ImageFolder(
    f'{data_dir}/train',
    transform=train_transforms
)
val_dataset = datasets.ImageFolder(
    f'{data_dir}/val',
    transform=val_transforms
)

train_loader = DataLoader(train_dataset, batch_size=32, shuffle=True, num_workers=4)
val_loader = DataLoader(val_dataset, batch_size=64, shuffle=False, num_workers=4)

print(f"训练集：{len(train_dataset)} 张")
print(f"验证集：{len(val_dataset)} 张")
print(f"类别：{train_dataset.classes}")


# 2. 创建 CNN 模型
class SimpleCNN(nn.Module):
    def __init__(self) -> None:
        super(SimpleCNN, self).__init__()

        self.conv1 = nn.Sequential(
            nn.Conv2d(3, 64, kernel_size=3, padding=1),
            nn.BatchNorm2d(64),
            nn.ReLU(),
            nn.MaxPool2d(2, 2)
        )

        self.conv2 = nn.Sequential(
            nn.Conv2d(64, 128, kernel_size=3, padding=1),
            nn.BatchNorm2d(128),
            nn.ReLU(),
            nn.MaxPool2d(2, 2)
        )

        self.conv3 = nn.Sequential(
            nn.Conv2d(128, 256, kernel_size=3, padding=1),
            nn.BatchNorm2d(256),
            nn.ReLU(),
            nn.MaxPool2d(2, 2)
        )

        self.conv4 = nn.Sequential(
            nn.Conv2d(256, 512, kernel_size=3, padding=1),
            nn.BatchNorm2d(512),
            nn.ReLU(),
            nn.MaxPool2d(2, 2)
        )

        self.fc = nn.Sequential(
            nn.Flatten(),
            nn.Linear(512 * 14 * 14, 1024),
            nn.ReLU(),
            nn.Dropout(0.5),
            nn.Linear(1024, 2)
        )

    def forward(self, x: Tensor) -> Tensor:
        x = self.conv1(x)
        x = self.conv2(x)
        x = self.conv3(x)
        x = self.conv4(x)
        x = self.fc(x)
        return x


model = SimpleCNN()
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
model = model.to(device)

print(f"模型参数量：{sum(p.numel() for p in model.parameters()):,}")


# 3. 训练配置
criterion = nn.CrossEntropyLoss()
optimizer = optim.Adam(model.parameters(), lr=0.001)
scheduler = optim.lr_scheduler.ReduceLROnPlateau(
    optimizer, mode='min', factor=0.5, patience=3, verbose=True
)


# 4. 训练函数
def train_one_epoch(model, loader, optimizer, criterion, device):
    model.train()
    total_loss = 0
    correct = 0
    total = 0

    for inputs, labels in loader:
        inputs, labels = inputs.to(device), labels.to(device)

        optimizer.zero_grad()
        outputs = model(inputs)
        loss = criterion(outputs, labels)
        loss.backward()
        optimizer.step()

        total_loss += loss.item() * inputs.size(0)
        _, predicted = outputs.max(1)
        total += labels.size(0)
        correct += predicted.eq(labels).sum().item()

    return total_loss / total, 100. * correct / total


def evaluate(model, loader, criterion, device):
    model.eval()
    total_loss = 0
    correct = 0
    total = 0

    with torch.no_grad():
        for inputs, labels in loader:
            inputs, labels = inputs.to(device), labels.to(device)
            outputs = model(inputs)
            loss = criterion(outputs, labels)

            total_loss += loss.item() * inputs.size(0)
            _, predicted = outputs.max(1)
            total += labels.size(0)
            correct += predicted.eq(labels).sum().item()

    return total_loss / total, 100. * correct / total


# 5. 开始训练
num_epochs = 30
best_acc = 0
history = {'train_loss': [], 'train_acc': [], 'val_loss': [], 'val_acc': []}

for epoch in range(num_epochs):
    train_loss, train_acc = train_one_epoch(model, train_loader, optimizer, criterion, device)
    val_loss, val_acc = evaluate(model, val_loader, criterion, device)

    scheduler.step(val_loss)

    history['train_loss'].append(train_loss)
    history['train_acc'].append(train_acc)
    history['val_loss'].append(val_loss)
    history['val_acc'].append(val_acc)

    print(f"Epoch {epoch+1}/{num_epochs}")
    print(f"  Train Loss: {train_loss:.4f}, Acc: {train_acc:.2f}%")
    print(f"  Val Loss: {val_loss:.4f}, Acc: {val_acc:.2f}%")

    # 保存最佳模型
    if val_acc > best_acc:
        best_acc = val_acc
        torch.save(model.state_dict(), 'best_cnn.pth')
        print(f"  ✓ 保存最佳模型 (Acc: {val_acc:.2f}%)")

print(f"\n最佳验证准确率：{best_acc:.2f}%")


# 6. 可视化训练过程
plt.figure(figsize=(12, 5))

plt.subplot(1, 2, 1)
plt.plot(history['train_loss'], label='Train Loss')
plt.plot(history['val_loss'], label='Val Loss')
plt.xlabel('Epoch')
plt.ylabel('Loss')
plt.legend()
plt.title('Loss Curve')

plt.subplot(1, 2, 2)
plt.plot(history['train_acc'], label='Train Acc')
plt.plot(history['val_acc'], label='Val Acc')
plt.xlabel('Epoch')
plt.ylabel('Accuracy')
plt.legend()
plt.title('Accuracy Curve')

plt.tight_layout()
plt.show()
```

---

### 39.3 使用迁移学习

```python
import torch
import torch.nn as nn
import torch.optim as optim
from torchvision import models

# 方法 1：使用预训练 ResNet18（推荐）
def create_resnet18_finetune(num_classes=2, freeze_backbone=True):
    """创建微调的 ResNet18"""

    # 加载预训练权重
    weights = models.ResNet18_Weights.IMAGENET1K_V1
    model = models.resnet18(weights=weights)

    # 冻结骨干网络
    if freeze_backbone:
        for param in model.parameters():
            param.requires_grad = False

    # 替换分类头
    num_features = model.fc.in_features
    model.fc = nn.Linear(num_features, num_classes)

    return model


# 方法 2：使用 EfficientNet（更高效的架构）
def create_efficientnet_finetune(num_classes=2):
    """创建微调的 EfficientNet-B0"""

    weights = models.EfficientNet_B0_Weights.IMAGENET1K_V1
    model = models.efficientnet_b0(weights=weights)

    # 冻结
    for param in model.parameters():
        param.requires_grad = False

    # 替换分类器
    num_features = model.classifier[1].in_features
    model.classifier = nn.Sequential(
        nn.Dropout(0.2),
        nn.Linear(num_features, num_classes)
    )

    return model


# 方法 3：特征提取（不微调）
def create_feature_extractor(num_classes=2):
    """仅使用预训练模型作为特征提取器"""

    weights = models.ResNet18_Weights.IMAGENET1K_V1
    model = models.resnet18(weights=weights)

    # 完全冻结
    for param in model.parameters():
        param.requires_grad = False

    # 替换分类头
    num_features = model.fc.in_features
    model.fc = nn.Linear(num_features, num_classes)

    return model


# 创建模型
model = create_resnet18_finetune(num_classes=2, freeze_backbone=False)
model = model.to(device)

# 训练配置（微调时使用较小的学习率）
criterion = nn.CrossEntropyLoss()
optimizer = optim.Adam(model.parameters(), lr=0.0001)
scheduler = optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=30)

# 训练（代码同上）
# ...
```

---

### 39.4 模型评估与预测

```python
from sklearn.metrics import classification_report, confusion_matrix
import numpy as np

def evaluate_model(model, loader, device, class_names):
    """详细评估模型"""
    model.eval()
    all_preds = []
    all_labels = []

    with torch.no_grad():
        for inputs, labels in loader:
            inputs = inputs.to(device)
            outputs = model(inputs)
            _, preds = torch.max(outputs, 1)
            all_preds.extend(preds.cpu().numpy())
            all_labels.extend(labels.numpy())

    # 分类报告
    print("Classification Report:")
    print(classification_report(all_labels, all_preds, target_names=class_names))

    # 混淆矩阵
    cm = confusion_matrix(all_labels, all_preds)
    print(f"\nConfusion Matrix:\n{cm}")

    return all_preds


def predict_image(model, image_path, device, class_names):
    """预测单张图像"""
    from PIL import Image

    model.eval()

    # 加载并预处理图像
    transform = transforms.Compose([
        transforms.Resize(256),
        transforms.CenterCrop(224),
        transforms.ToTensor(),
        transforms.Normalize([0.485, 0.456, 0.406],
                            [0.229, 0.224, 0.225])
    ])

    image = Image.open(image_path).convert('RGB')
    input_tensor = transform(image).unsqueeze(0).to(device)

    # 预测
    with torch.no_grad():
        output = model(input_tensor)
        _, pred = torch.max(output, 1)
        prob = torch.softmax(output, dim=1)[0]

    print(f"图像：{image_path}")
    print(f"预测：{class_names[pred]} (置信度：{prob[pred]:.2%})")

    return class_names[pred], prob[pred].item()


# 使用示例
evaluate_model(model, val_loader, device, train_dataset.classes)
predict_image(model, 'data/test/cat1.jpg', device, train_dataset.classes)
```

---

## 项目二：文本生成 - 唐诗生成

### 39.5 项目概述

使用 LSTM 构建语言模型，学习唐诗的韵律和风格，生成新的诗句。

**项目目标：**
- 掌握语言模型原理
- 学习序列生成技术
- 理解温度采样策略

---

### 39.6 数据准备

```python
import torch
from torch.utils.data import Dataset, DataLoader
from collections import Counter

class PoetryDataset(Dataset):
    """唐诗数据集"""

    def __init__(self, file_path, seq_length=50):
        with open(file_path, 'r', encoding='utf-8') as f:
            lines = f.readlines()

        # 清理数据
        poems = []
        for line in lines:
            line = line.strip()
            if len(line) >= seq_length:
                poems.append(line[:seq_length + 1])  # +1 用于预测

        self.poems = poems
        self.seq_length = seq_length

        # 构建词汇表
        chars = Counter(''.join(poems))
        self.char_to_idx = {'<pad>': 0, '<unk>': 1}
        for i, char in enumerate(chars.keys()):
            self.char_to_idx[char] = i + 2
        self.idx_to_char = {v: k for k, v in self.char_to_idx.items()}
        self.vocab_size = len(self.char_to_idx)

    def __len__(self):
        return len(self.poems)

    def __getitem__(self, idx):
        poem = self.poems[idx]
        x = [self.char_to_idx.get(c, 1) for c in poem[:-1]]
        y = [self.char_to_idx.get(c, 1) for c in poem[1:]]
        return torch.tensor(x), torch.tensor(y)


# 加载数据
dataset = PoetryDataset('data/poems.txt', seq_length=50)
train_loader = DataLoader(dataset, batch_size=64, shuffle=True, num_workers=4)

print(f"诗歌数量：{len(dataset)}")
print(f"词汇表大小：{dataset.vocab_size}")
```

---

### 39.7 创建语言模型

```python
import torch.nn as nn
from torch import Tensor
from typing import tuple, Any

class PoetryLSTM(nn.Module):
    """唐诗生成 LSTM 模型"""

    def __init__(self, vocab_size: int, embedding_dim: int, hidden_dim: int, num_layers: int = 2, dropout: float = 0.2) -> None:
        super(PoetryLSTM, self).__init__()

        self.embedding = nn.Embedding(vocab_size, embedding_dim)
        self.lstm = nn.LSTM(
            embedding_dim,
            hidden_dim,
            num_layers=num_layers,
            batch_first=True,
            dropout=dropout if num_layers > 1 else 0
        )
        self.fc = nn.Linear(hidden_dim, vocab_size)
        self.dropout = nn.Dropout(dropout)

    def forward(self, x: Tensor, hidden: tuple[Tensor, Tensor] | None = None) -> tuple[Tensor, tuple[Tensor, Tensor]]:
        embedded: Tensor = self.dropout(self.embedding(x))

        output: Tensor
        new_hidden: tuple[Tensor, Tensor]
        output, new_hidden = self.lstm(embedded, hidden)

        output = self.dropout(output)
        output = self.fc(output)

        return output, new_hidden

    def generate(self, start_chars, length=50, temperature=1.0, device='cpu'):
        """生成诗歌"""
        self.eval()

        # 转换起始字符为索引
        chars = list(start_chars)
        x = [dataset.char_to_idx.get(c, 1) for c in chars]
        x = torch.tensor([x]).to(device)

        hidden = None

        with torch.no_grad():
            for _ in range(length):
                output, hidden = self(x, hidden)
                # output: [1, seq_len, vocab_size]

                # 取最后一个时间步的输出
                logits = output[:, -1, :] / temperature
                # [1, vocab_size]

                # 采样下一个字符
                probs = torch.softmax(logits, dim=-1)
                next_idx = torch.multinomial(probs, 1)

                # 添加到输入
                x = torch.cat([x, next_idx], dim=1)

        # 转换为字符串
        generated = ''.join([dataset.idx_to_char[idx.item()] for idx in x[0]])
        return generated


# 创建模型
model = PoetryLSTM(
    vocab_size=dataset.vocab_size,
    embedding_dim=256,
    hidden_dim=512,
    num_layers=3,
    dropout=0.3
)

device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
model = model.to(device)
```

---

### 39.8 训练与生成

```python
# 训练
criterion = nn.CrossEntropyLoss()
optimizer = optim.Adam(model.parameters(), lr=0.001)
scheduler = optim.lr_scheduler.ReduceLROnPlateau(optimizer, mode='min', patience=5)

num_epochs = 100

for epoch in range(num_epochs):
    model.train()
    total_loss = 0

    for x, y in train_loader:
        x, y = x.to(device), y.to(device)

        optimizer.zero_grad()
        output, _ = model(x)

        # 计算损失（展平）
        loss = criterion(output.view(-1, dataset.vocab_size), y.view(-1))
        loss.backward()

        # 梯度裁剪（防止梯度爆炸）
        torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)

        optimizer.step()
        total_loss += loss.item()

    avg_loss = total_loss / len(train_loader)
    scheduler.step(avg_loss)

    if (epoch + 1) % 10 == 0:
        print(f"Epoch {epoch+1}, Loss: {avg_loss:.4f}")

        # 生成示例
        model.eval()
        with torch.no_grad():
            generated = model.generate("床前明月", length=30, temperature=0.8, device=device)
            print(f"生成：{generated}")

# 保存模型
torch.save({
    'model_state_dict': model.state_dict(),
    'vocab_size': dataset.vocab_size,
    'char_to_idx': dataset.char_to_idx,
    'idx_to_char': dataset.idx_to_char
}, 'poetry_lstm.pth')
```

---

### 39.9 温度采样

```python
# 不同温度下的生成效果
temperatures = [0.5, 0.8, 1.0, 1.2]
start_phrase = "春眠不觉晓"

for temp in temperatures:
    generated = model.generate(start_phrase, length=20, temperature=temp, device=device)
    print(f"Temperature {temp}: {generated}")

# 温度解释：
# 0.5: 保守、重复性强
# 0.8: 平衡、推荐
# 1.0: 标准采样
# 1.2: 创造性高、可能不通顺
```

---

## 项目三：迁移学习实战

### 39.10 使用预训练模型

```python
import torch
from torchvision import models
import torch.nn as nn

# 常见预训练模型对比
pretrained_models = {
    'resnet18': models.resnet18,
    'resnet50': models.resnet50,
    'efficientnet_b0': models.efficientnet_b0,
    'mobilenet_v2': models.mobilenet_v2,  # 移动端友好
    'vit_b_16': models.vit_b_16,  # Vision Transformer
}

# 加载预训练模型的最佳实践
def load_pretrained_model(model_name='resnet18', num_classes=2, freeze=True):
    """加载预训练模型的通用函数"""

    if model_name == 'resnet18':
        weights = models.ResNet18_Weights.IMAGENET1K_V1
        model = models.resnet18(weights=weights)
        if freeze:
            for param in model.parameters():
                param.requires_grad = False
        model.fc = nn.Linear(model.fc.in_features, num_classes)

    elif model_name == 'efficientnet_b0':
        weights = models.EfficientNet_B0_Weights.IMAGENET1K_V1
        model = models.efficientnet_b0(weights=weights)
        if freeze:
            for param in model.parameters():
                param.requires_grad = False
        model.classifier[1] = nn.Linear(model.classifier[1].in_features, num_classes)

    return model


# 两阶段训练策略
def two_stage_training(model, train_loader, val_loader, device, num_epochs=30):
    """
    阶段 1：只训练分类头（快速收敛）
    阶段 2：微调骨干网络（提升性能）
    """

    criterion = nn.CrossEntropyLoss()

    # 阶段 1：训练分类头
    print("阶段 1：训练分类头...")
    optimizer = optim.Adam(model.fc.parameters(), lr=0.001)

    for epoch in range(10):
        train_loss, train_acc = train_epoch(model, train_loader, optimizer, criterion, device)
        print(f"Epoch {epoch+1}: Loss={train_loss:.4f}, Acc={train_acc:.2f}%")

    # 阶段 2：微调骨干网络
    print("\n阶段 2：微调骨干网络...")
    for param in model.parameters():
        param.requires_grad = True

    optimizer = optim.Adam(model.parameters(), lr=0.0001)
    scheduler = optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=20)

    for epoch in range(20):
        train_loss, train_acc = train_epoch(model, train_loader, optimizer, criterion, device)
        scheduler.step()
        print(f"Epoch {epoch+1}: Loss={train_loss:.4f}, Acc={train_acc:.2f}%")

    return model
```

---

## 本章小结

```
┌─────────────────────────────────────────────────────────────┐
│                      第 39 章 知识要点                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   项目一：图像分类（猫狗识别）                               │
│   ✓ 从零训练 CNN：卷积层、池化层、全连接层                  │
│   ✓ 数据增强：RandomCrop、Flip、Rotation、ColorJitter       │
│   ✓ 迁移学习：预训练 ResNet、冻结骨干、微调分类头           │
│   ✓ 模型评估：准确率、混淆矩阵、分类报告                   │
│                                                             │
│   项目二：文本生成（唐诗生成）                               │
│   ✓ 语言模型：预测下一个词                                 │
│   ✓ LSTM 生成：逐词生成、隐藏状态传递                        │
│   ✓ 温度采样：控制生成多样性                                │
│   ✓ 数据准备：字符级词汇表、序列数据集                     │
│                                                             │
│   项目三：迁移学习实战                                       │
│   ✓ 预训练模型：ResNet、EfficientNet、MobileNet、ViT        │
│   ✓ 两阶段训练：先训练分类头，再微调骨干                   │
│   ✓ 学习率策略：阶段 1 高 LR，阶段 2 低 LR                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```
