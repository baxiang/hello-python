[project]
name = "fastapi-demo"
version = "0.1.0"
description = "FastAPI Web 框架示例项目"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.109.0", "uvicorn>=0.27.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]