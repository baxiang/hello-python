[project]
name = "flask-demo"
version = "0.1.0"
description = "Flask Web 框架示例项目"
requires-python = ">=3.11"
dependencies = ["flask>=3.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]