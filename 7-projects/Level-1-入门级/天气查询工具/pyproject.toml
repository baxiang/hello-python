[project]
name = "weather-query"
version = "0.1.0"
description = "Level 1: Simple weather query script"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
