# 08-认证授权

> Python 3.11+

本章讲解 Flask 用户认证和权限管理。

---

## 概念铺垫

Flask 认证授权体系包括：**Flask-Login**（基于客户端签名 Cookie 的会话认证）、**Flask-JWT-Extended**（无状态 Token 认证）、以及**基于角色的访问控制（RBAC）模型**。会话认证通过 session 存储 `user_id` 并由 `user_loader` 回调加载用户；JWT 通过 Header.Payload.Signature 三段式 HMAC 签名确保 Token 不可伪造；RBAC 通过角色-权限矩阵将用户与权限解耦。

密码存储必须使用 bcrypt/argon2（计算慢、内置盐值、抗彩虹表），绝不能使用 MD5/SHA1。

---

### L1 理解层：会用

## 第一部分：Flask-Login

### 1.1 实际场景

用户登录后需要记住登录状态，访问个人中心时需要检查是否已登录。

**问题：如何实现用户登录和会话管理？**

### 1.2 安装

```bash
pip install flask-login flask-bcrypt
```

### 1.3 配置

```python
from flask import Flask
from flask_login import LoginManager

app: Flask = Flask(__name__)
app.config["SECRET_KEY"] = "secret-key"

login_manager: LoginManager = LoginManager()
login_manager.init_app(app)
login_manager.login_view = "auth.login"
```

### 1.4 用户模型

```python
from flask_login import UserMixin
from flask_sqlalchemy import SQLAlchemy

db: SQLAlchemy = SQLAlchemy()

class User(UserMixin, db.Model):
    id: int = db.Column(db.Integer, primary_key=True)
    username: str = db.Column(db.String(80), unique=True)
    email: str = db.Column(db.String(120), unique=True)
    password_hash: str = db.Column(db.String(200))
    is_active: bool = db.Column(db.Boolean, default=True)
    
    def set_password(self, password: str) -> None:
        from flask_bcrypt import Bcrypt
        self.password_hash = Bcrypt().generate_password_hash(password).decode()
    
    def check_password(self, password: str) -> bool:
        from flask_bcrypt import Bcrypt
        return Bcrypt().check_password_hash(self.password_hash, password)
```

### 1.5 加载用户

```python
@login_manager.user_loader
def load_user(user_id: str) -> User | None:
    return User.query.get(int(user_id))
```

---

## 第二部分：登录流程

### 2.1 实际场景

用户在登录页面输入用户名密码，验证成功后跳转到首页。

**问题：如何实现登录和登出功能？**

### 2.2 登录视图

```python
from flask_login import login_user, logout_user, login_required, current_user
from flask import request, redirect, url_for, flash, render_template

@app.route("/login", methods=["GET", "POST"])
def login() -> str:
    if request.method == "POST":
        username: str = request.form.get("username", "")
        password: str = request.form.get("password", "")
        
        user: User | None = User.query.filter_by(username=username).first()
        
        if user and user.check_password(password):
            login_user(user)
            return redirect(url_for("dashboard"))
        else:
            flash("用户名或密码错误")
    
    return render_template("auth/login.html")

@app.route("/logout")
@login_required
def logout() -> str:
    logout_user()
    return redirect(url_for("login"))
```

### 2.3 受保护路由

```python
@app.route("/dashboard")
@login_required
def dashboard() -> str:
    return f"欢迎, {current_user.username}!"
```

---

## 第三部分：JWT 认证

### 3.1 实际场景

移动端应用需要 API 认证，不适合使用 session，需要使用 Token 认证。

**问题：如何实现 JWT Token 认证？**

### 3.2 安装

```bash
pip install flask-jwt-extended
```

### 3.3 配置

```python
from flask_jwt_extended import JWTManager

app.config["JWT_SECRET_KEY"] = "jwt-secret"
app.config["JWT_ACCESS_TOKEN_EXPIRES"] = 3600

jwt: JWTManager = JWTManager(app)
```

### 3.4 使用

```python
from flask_jwt_extended import create_access_token, jwt_required, get_jwt_identity
from typing import Any

@app.route("/login", methods=["POST"])
def login() -> dict[str, str] | tuple[dict[str, str], int]:
    data: dict[str, Any] = request.get_json()
    user: User | None = User.query.filter_by(username=data["username"]).first()
    
    if user and user.check_password(data["password"]):
        token: str = create_access_token(identity=user.id)
        return {"token": token}
    
    return {"error": "Invalid credentials"}, 401

@app.route("/protected")
@jwt_required()
def protected() -> dict[str, str]:
    user_id: int = get_jwt_identity()
    user: User | None = User.query.get(user_id)
    return {"user": user.username if user else "Unknown"}
```

---

## 第四部分：权限管理

### 4.1 实际场景

管理员可以访问后台管理页面，普通用户没有权限。

**问题：如何实现基于角色的权限控制？**

### 4.2 角色定义

```python
class Role(db.Model):
    id: int = db.Column(db.Integer, primary_key=True)
    name: str = db.Column(db.String(50), unique=True)
    users = db.relationship("User", backref="role", lazy="dynamic")

class User(db.Model):
    id: int = db.Column(db.Integer, primary_key=True)
    username: str = db.Column(db.String(80))
    role_id: int = db.Column(db.Integer, db.ForeignKey("role.id"))
```

### 4.3 权限装饰器

```python
from functools import wraps
from flask import abort
from flask_login import current_user

def admin_required(f):
    @wraps(f)
    def decorated_function(*args, **kwargs):
        if not current_user.is_authenticated:
            abort(401)
        if current_user.role.name != "admin":
            abort(403)
        return f(*args, **kwargs)
    return decorated_function

@app.route("/admin")
@admin_required
def admin_panel() -> str:
    return "Admin Panel"
```

---

## 第五部分：完整示例

```python
from flask import Flask, request, jsonify
from flask_sqlalchemy import SQLAlchemy
from flask_login import LoginManager, UserMixin, login_user, login_required, logout_user, current_user
from flask_bcrypt import Bcrypt
from flask_jwt_extended import JWTManager, create_access_token, jwt_required, get_jwt_identity
from typing import Any

app: Flask = Flask(__name__)
app.config["SECRET_KEY"] = "secret"
app.config["JWT_SECRET_KEY"] = "jwt-secret"
app.config["SQLALCHEMY_DATABASE_URI"] = "sqlite:///app.db"

db: SQLAlchemy = SQLAlchemy(app)
bcrypt: Bcrypt = Bcrypt(app)
login_manager: LoginManager = LoginManager(app)
jwt: JWTManager = JWTManager(app)

# 模型
class User(UserMixin, db.Model):
    id: int = db.Column(db.Integer, primary_key=True)
    username: str = db.Column(db.String(80), unique=True)
    email: str = db.Column(db.String(120), unique=True)
    password_hash: str = db.Column(db.String(200))
    role: str = db.Column(db.String(20), default="user")
    
    def set_password(self, password: str) -> None:
        self.password_hash = bcrypt.generate_password_hash(password).decode()
    
    def check_password(self, password: str) -> bool:
        return bcrypt.check_password_hash(self.password_hash, password)

@login_manager.user_loader
def load_user(user_id: str) -> User | None:
    return User.query.get(int(user_id))

# 路由
@app.route("/register", methods=["POST"])
def register() -> tuple[dict[str, str], int]:
    data: dict[str, Any] = request.get_json()
    user: User = User(username=data["username"], email=data["email"])
    user.set_password(data["password"])
    db.session.add(user)
    db.session.commit()
    return {"message": "Created"}, 201

@app.route("/login", methods=["POST"])
def login() -> dict[str, str] | tuple[dict[str, str], int]:
    data: dict[str, Any] = request.get_json()
    user: User | None = User.query.filter_by(username=data["username"]).first()
    
    if user and user.check_password(data["password"]):
        token: str = create_access_token(identity=user.id)
        return {"token": token}
    
    return {"error": "Invalid"}, 401

@app.route("/profile")
@jwt_required()
def profile() -> dict[str, str]:
    user_id: int = get_jwt_identity()
    user: User | None = User.query.get(user_id)
    return {"username": user.username, "email": user.email} if user else {"error": "Not found"}

if __name__ == "__main__":
    with app.app_context():
        db.create_all()
    app.run(debug=True)
```

---

### L2 实践层：用好

| 做法 | 原因 | 示例 |
|------|------|------|
| bcrypt rounds >= 12 | 增加暴力破解成本 | `bcrypt.gensalt(rounds=12)` |
| JWT 过期时间设短（15min） | Token 泄露后影响窗口小 | `JWT_ACCESS_TOKEN_EXPIRES = 900` |
| 刷新 Token 独立存储 | 访问 Token 与刷新 Token 分离 | 访问 token 15min，刷新 token 7d |
| `login_required` + 自定义权限装饰器 | 认证 + 授权双重保护 | `@admin_required` |
| `session.clear()` 完整登出 | 清除所有 session 数据 | 而非 `session.pop("user")` |

#### 反模式

```python
# ❌ 错误：使用 MD5/SHA1 存密码
import hashlib
weak_hash = hashlib.md5(password.encode()).hexdigest()

# ✅ 正确：使用 bcrypt
import bcrypt
hashed = bcrypt.hashpw(password.encode(), bcrypt.gensalt())

# ❌ 错误：客户端 session 存敏感信息
session["password"] = user.password  # Cookie 可被解码

# ✅ 正确：仅存标识
session["user_id"] = user.id

# ❌ 错误：JWT 不设过期时间
token = create_access_token(identity=user_id)

# ✅ 正确：设置合理过期
token = create_access_token(identity=user_id, expires_delta=timedelta(minutes=15))
```

---

### L3 专家层：深入

## 第六部分：L3 专家层

### 6.1 JWT 的签名与验证流程（Header.Payload.Signature）

JWT（JSON Web Token）由三部分组成，每部分使用 Base64Url 编码，以 `.` 分隔。其安全性依赖于 HMAC 或 RSA 签名。

**JWT 结构：**

```
  Header.Payload.Signature
  +------------------+------------------+------------------+
  |     Header       |     Payload      |    Signature     |
  | (算法+类型)       | (声明+数据)       | (HMAC/RSA 签名)  |
  +------------------+------------------+------------------+
  | Base64Url        | Base64Url        | Base64Url        |
  +------------------+------------------+------------------+
```

**签名生成流程：**

```
  +-------------+    Base64Url    +------------------+
  |   Header    | --------------> |  encodedHeader   |
  | {"alg":"HS256",|              +--------+---------+
  |  "typ":"JWT"} |                       |
  +-------------+                       |
                                        v
                               +------------------+
                               |  encodedHeader + |
                               |  "." +           |
                               |  encodedPayload  |
                               +--------+---------+
                                        |
                                        v
  +-------------+               +------------------+
  |  SECRET_KEY  |-------------->| HMAC-SHA256      |
  +-------------+               | (签名计算)        |
                                +--------+---------+
                                         |
  +-------------+                        |
  |  Payload    |    Base64Url           |
  | {"sub":"1", | -------------->       |
  |  "exp":...} |                       |
  +-------------+                       v
                               +------------------+
                               |    Signature     |
                               +------------------+
```

```python
import hmac
import hashlib
import base64
import json
import time
from typing import Any

def base64url_encode(data: bytes) -> str:
    """Base64Url 编码（无填充）"""
    return base64.urlsafe_b64encode(data).rstrip(b"=").decode("utf-8")

def base64url_decode(s: str) -> bytes:
    """Base64Url 解码"""
    s_padded: str = s + "=" * (4 - len(s) % 4)
    return base64.urlsafe_b64decode(s_padded)

def create_jwt(payload: dict[str, Any], secret: str, algorithm: str = "HS256") -> str:
    """创建 JWT Token"""
    # Header
    header: dict[str, str] = {"alg": algorithm, "typ": "JWT"}
    encoded_header: str = base64url_encode(json.dumps(header, separators=(",", ":")).encode())
    
    # Payload
    encoded_payload: str = base64url_encode(json.dumps(payload, separators=(",", ":")).encode())
    
    # Signature
    signing_input: str = f"{encoded_header}.{encoded_payload}"
    if algorithm == "HS256":
        signature: bytes = hmac.new(
            secret.encode("utf-8"),
            signing_input.encode("utf-8"),
            hashlib.sha256
        ).digest()
    else:
        raise ValueError(f"Unsupported algorithm: {algorithm}")
    
    encoded_signature: str = base64url_encode(signature)
    
    return f"{encoded_header}.{encoded_payload}.{encoded_signature}"

def verify_jwt(token: str, secret: str) -> dict[str, Any] | None:
    """验证并解析 JWT Token"""
    try:
        header_b64, payload_b64, signature_b64 = token.split(".")
        
        # 验证签名
        signing_input: str = f"{header_b64}.{payload_b64}"
        expected_sig: bytes = hmac.new(
            secret.encode("utf-8"),
            signing_input.encode("utf-8"),
            hashlib.sha256
        ).digest()
        
        actual_sig: bytes = base64url_decode(signature_b64)
        if not hmac.compare_digest(expected_sig, actual_sig):
            return None
        
        # 解析 Payload
        payload: dict[str, Any] = json.loads(base64url_decode(payload_b64))
        
        # 检查过期时间
        if "exp" in payload and payload["exp"] < time.time():
            return None
        
        return payload
    except (ValueError, json.JSONDecodeError):
        return None
```

**关键安全要点：**

| 安全项 | 说明 | 风险 |
|--------|------|------|
| `alg: none` 攻击 | 攻击者将 Header 算法设为 `none` 绕过签名 | 必须拒绝 `alg=none` |
| 密钥泄露 | 服务端 SECRET_KEY 泄露 = 可伪造任意 Token | 使用强随机密钥，定期轮换 |
| Token 过期 | 未设置 `exp` 的 Token 永久有效 | 设置合理的过期时间 |
| 敏感信息 | Payload 仅 Base64 编码，可被任何人读取 | 不要在 Payload 存密码等敏感数据 |

### 6.2 Flask-Login 的 session 管理机制

Flask-Login 基于 Flask 的 session（默认使用客户端签名 cookie）实现用户状态管理，核心在于 session 中存储 `user_id`。

**Session 管理流程：**

```
  登录请求                           后续请求
     |                                 |
     v                                 v
  +-------------+              +-------------+
  | 验证用户凭证 |              | 读取 Cookie  |
  +-------------+              +-------------+
     |                                 |
     v                                 v
  +-------------+              +-------------+
  | login_user()|              | session 中   |
  | 设置 session |              | 提取 user_id |
  | {'user_id':  |              +-------------+
  |   '123'}     |                       |
  +-------------+                       v
     |                          +-------------+
     v                          | load_user() |
  +-------------+               | (回调函数)   |
  | Set-Cookie  |               +-------------+
  | (签名后的    |                       |
  |  session)   |                       v
  +-------------+               +-------------+
                                | current_user|
                                | 代理对象     |
                                +-------------+
```

```python
from flask import session, request, g
from typing import Any, Callable
from functools import wraps

class SimpleLoginManager:
    """Flask-Login 核心机制简化演示"""
    
    def __init__(self) -> None:
        self._user_loader: Callable | None = None
    
    def user_loader(self, callback: Callable) -> Callable:
        """注册用户加载回调"""
        self._user_loader = callback
        return callback
    
    def login_user(self, user: Any, remember: bool = False) -> bool:
        """登录用户（设置 session）"""
        user_id: str = str(user.get_id())
        session["user_id"] = user_id
        if remember:
            session.permanent = True  # 设置长期有效
        return True
    
    def logout_user(self) -> None:
        """登出用户（清除 session）"""
        session.pop("user_id", None)
    
    def get_current_user(self) -> Any | None:
        """获取当前用户（每次请求调用）"""
        user_id: str | None = session.get("user_id")
        if user_id is None:
            return None
        if self._user_loader is not None:
            return self._user_loader(user_id)
        return None
    
    def login_required(self, f: Callable) -> Callable:
        """登录保护装饰器"""
        @wraps(f)
        def decorated_function(*args: Any, **kwargs: Any) -> Any:
            if self.get_current_user() is None:
                from flask import redirect, url_for
                return redirect(url_for("login"))
            return f(*args, **kwargs)
        return decorated_function
```

**Session 安全机制：**

| 机制 | 说明 | Flask 默认行为 |
|------|------|----------------|
| 签名（Signature） | 防止客户端篡改 session 数据 | 使用 `SECRET_KEY` 签名 |
| 加密（Encryption） | 隐藏 session 内容 | 默认**不加密**（仅签名） |
| HttpOnly | 阻止 JavaScript 读取 Cookie | Flask session Cookie 默认启用 |
| Secure | 仅 HTTPS 传输 | 需手动设置 `SESSION_COOKIE_SECURE = True` |
| SameSite | 防止 CSRF 攻击 | 默认 `Lax` |

### 6.3 基于角色的访问控制（RBAC）模型

RBAC（Role-Based Access Control）通过角色作为中介，将用户与权限解耦，实现灵活的权限管理。

**RBAC 数据模型：**

```
  +----------+       +----------+       +----------+
  |   User   |       |   Role   |       |Permission|
  +----------+       +----------+       +----------+
  | id       |  M:N  | id       |  M:N  | id       |
  | username |<----->| name     |<----->| name     |
  | email    |       | 描述      |       | resource |
  | ...      |       | ...      |       | action   |
  +----------+       +----------+       +----------+
  
  关系表:                    关系表:
  user_roles                role_permissions
  +--------+-------+        +-----------+-------------+
  |user_id |role_id|        |role_id    |permission_id|
  +--------+-------+        +-----------+-------------+
```

```python
from enum import Enum
from typing import Callable, Any
from functools import wraps
from flask import abort

class Action(Enum):
    """权限动作"""
    READ = "read"
    WRITE = "write"
    DELETE = "delete"
    ADMIN = "admin"

class PermissionChecker:
    """RBAC 权限检查器"""
    
    def __init__(self) -> None:
        # role -> set of permissions
        self._role_permissions: dict[str, set[Action]] = {
            "admin": {Action.READ, Action.WRITE, Action.DELETE, Action.ADMIN},
            "editor": {Action.READ, Action.WRITE},
            "viewer": {Action.READ},
        }
    
    def has_permission(self, role: str, required: Action) -> bool:
        """检查角色是否有指定权限"""
        permissions: set[Action] = self._role_permissions.get(role, set())
        return required in permissions
    
    def require_permission(self, action: Action) -> Callable:
        """权限装饰器工厂"""
        def decorator(f: Callable) -> Callable:
            @wraps(f)
            def wrapper(*args: Any, **kwargs: Any) -> Any:
                from flask_login import current_user
                if not current_user.is_authenticated:
                    abort(401)
                if not self.has_permission(current_user.role, action):
                    abort(403)
                return f(*args, **kwargs)
            return wrapper
        return decorator

# 使用示例
checker: PermissionChecker = PermissionChecker()

# @checker.require_permission(Action.DELETE)
# def delete_post(post_id: int) -> str:
#     ...
```

**设计动机：**

| RBAC 优势 | 说明 | 对比直接授权 |
|-----------|------|--------------|
| 可维护性 | 权限变更只需修改角色，不影响用户 | 直接授权需逐个修改用户 |
| 可扩展性 | 新增角色即可，无需改代码 | 硬编码权限难以扩展 |
| 最小权限 | 按角色分配最小必要权限 | 容易出现过度授权 |
| 审计友好 | 角色-权限关系清晰可审计 | 用户-权限关系散乱 |

### 6.4 知识关联

```
                    认证授权知识体系
                         |
        +----------------+----------------+
        |                |                |
     认证层          会话层          授权层
        |                |                |
   +----+----+      +----+----+      +----+----+
   | 密码哈希 |      | Session  |      |  角色   |
   | JWT     |      | Cookie   |      |  权限   |
   +----+----+      +----+----+      +----+----+
        |                |                |
        v                v                v
   +---------+      +---------+      +---------+
   | bcrypt  |      | 签名机制 |      | RBAC   |
   | HMAC    |      | HttpOnly|      | 装饰器  |
   | SHA256  |      | SameSite|      | 拦截器  |
   +---------+      +---------+      +---------+
                         |
                         v
                   +-----+-----+
                   | Token 管理 |
                   | 过期刷新   |
                   +-----+-----+
                         |
                   +-----+-----+      +---------+
                   | 访问令牌  |----->| 刷新令牌 |
                   | (短期)   |      | (长期)   |
                   +---------+      +---------+
```

| 知识点 | 说明 |
|---------|------|
| Flask-Login | 会话认证 |
| UserMixin | 用户模型混入 |
| bcrypt | 密码哈希 |
| JWT | Token 认证 |
| 权限装饰器 | 角色权限 |

---

## 第六部分：Session 认证深入

### 6.1 实际场景

需要理解 Flask session 的底层安全机制，以及在分布式部署中的 Session 共享问题。

**问题：Flask session 有何安全风险？在分布式系统中如何共享 session？**

### 6.2 Flask Session 完整生命周期

```
请求到达
  │
  ├── 1. 从 Cookie 读取 session（解码签名）
  │     session_cookie = request.cookies.get('session')
  │     session_data = itsdangerous.URLSafeTimedSerializer.loads(session_cookie)
  │
  ├── 2. 创建 SecureCookieSession 对象
  │     session = SecureCookieSession(session_data)
  │
  ├── 3. 视图函数访问 session（dict 代理）
  │     session['user_id'] = 123
  │     current_user = load_user(session.get('user_id'))
  │
  └── 4. 响应时序列化并签名 → 写入 Cookie
        signed_cookie = serializer.dumps(dict(session))
        response.set_cookie('session', signed_cookie, httponly=True)
```

### 6.3 服务端 Session（Redis 存储）

Flask 默认的客户端 session 有体积限制（Cookie 最大 4KB），且无法主动失效。

```python
# 使用 Flask-Session 实现服务端 session
# pip install flask-session redis

from flask import Flask, session
from flask_session import Session
import redis

app: Flask = Flask(__name__)

app.config["SESSION_TYPE"] = "redis"
app.config["SESSION_REDIS"] = redis.from_url("redis://localhost:6379")
app.config["SESSION_PERMANENT"] = False
app.config["SESSION_USE_SIGNER"] = True     # session id 签名防篡改
app.config["SESSION_KEY_PREFIX"] = "flask:" # Redis key 前缀

Session(app)

# 现在 session 数据存储在 Redis，Cookie 只存 session id
# 支持：主动失效、session 共享（多个 Worker 同读 Redis）、无 4KB 限制
```

### 6.4 分布式 Session 一致性

```python
# 多实例部署时 Session 一致性策略

# 策略一：粘性 Session（Sticky Session）
# Nginx 配置：
# upstream backend {
#     ip_hash;  # 同一 IP 路由到同一实例
#     server app1:8000;
#     server app2:8000;
# }
# 缺点：实例宕机 session 丢失

# 策略二：集中式 Session 存储（推荐）
# 使用 Redis/Memcached 存储 session，所有实例共享
# Flask-Session with Redis (如上所示)

# 策略三：无状态 JWT（最推荐用于 API）
# 不使用 session，用 JWT token 代替
# 每次请求携带 token，服务端不存储任何状态
```

---

## 第七部分：JWT Token 刷新与轮换

### 7.1 实际场景

Access Token 15 分钟过期后，用户需要重新登录体验很差。

**问题：如何实现无感 Token 刷新？**

### 7.2 Access + Refresh Token 双 Token 模式

```python
from flask_jwt_extended import (
    create_access_token, create_refresh_token,
    jwt_required, get_jwt_identity
)
from datetime import timedelta

@app.route("/login", methods=["POST"])
def login() -> dict:
    data = request.get_json()
    user = User.query.filter_by(username=data["username"]).first()
    if not user or not user.check_password(data["password"]):
        return {"error": "Invalid credentials"}, 401

    access_token = create_access_token(
        identity=user.id,
        expires_delta=timedelta(minutes=15)   # 短期
    )
    refresh_token = create_refresh_token(
        identity=user.id,
        expires_delta=timedelta(days=7)       # 长期
    )

    return {"access_token": access_token, "refresh_token": refresh_token}

@app.route("/refresh", methods=["POST"])
@jwt_required(refresh=True)  # 仅接受 refresh token
def refresh() -> dict:
    user_id = get_jwt_identity()
    new_access_token = create_access_token(identity=user_id)
    return {"access_token": new_access_token}
```

### 7.3 Token 轮换（Rotation）防重放攻击

```python
# Token 轮换：每次刷新时同时更换 refresh token
# 旧 refresh token 标记为已使用，防止被盗后重复使用

from flask_jwt_extended import get_jwt

import redis
r = redis.from_url("redis://localhost:6379")

@app.route("/refresh", methods=["POST"])
@jwt_required(refresh=True)
def refresh_with_rotation() -> dict:
    user_id = get_jwt_identity()
    jti = get_jwt()["jti"]  # JWT ID

    # 检查是否已被使用（防重放）
    if r.exists(f"used_jti:{jti}"):
        # Token 已被使用过！可能被盗
        r.setex(f"user_revoked:{user_id}", 86400 * 7, "1")
        return {"error": "Token reuse detected"}, 401

    # 标记旧 token 已使用
    r.setex(f"used_jti:{jti}", 86400 * 7, "1")

    # 生成新 Token 对
    new_access = create_access_token(identity=user_id)
    new_refresh = create_refresh_token(identity=user_id)

    return {"access_token": new_access, "refresh_token": new_refresh}
```

### 7.4 Token 撤销（黑名单）

```python
from flask_jwt_extended import JWTManager

@jwt.token_in_blocklist_loader
def check_if_token_revoked(jwt_header: dict, jwt_payload: dict) -> bool:
    """每次请求检查 Token 是否在黑名单中"""
    jti = jwt_payload["jti"]
    return r.exists(f"revoked:{jti}") == 1

# 撤销用户的所有 Token
def revoke_user_tokens(user_id: int) -> None:
    """撤销所有用户的活跃 Token"""
    r.setex(f"user_revoked:{user_id}", 86400 * 7, "1")
```

---

## 第八部分：OAuth2 / OIDC 集成

### 8.1 实际场景

支持用户用 Google/GitHub/微信账号登录你的应用。

**问题：如何集成第三方 OAuth2 登录？**

### 8.2 OAuth2 授权码流程

```
OAuth2 授权码流程（Authorization Code Grant）：
┌─────────────┐     ┌──────────────┐     ┌──────────────┐
│   用户浏览器  │     │  Flask 应用   │     │  OAuth 提供商  │
└──────┬──────┘     └──────┬───────┘     └──────┬───────┘
       │ 1. 点击"Google登录" │                    │
       │───────────────────>│                    │
       │                    │ 2. 重定向到授权页面  │
       │                    │────────────────────>│
       │ 3. 显示授权页面     │                    │
       │<──────────────────────── ─ ─ ─ ─ ─ ─ ─ ─│
       │ 4. 用户同意授权      │                    │
       │──────────────────────── ─ ─ ─ ─ ─ ─ ─ ─>│
       │                    │ 5. 回调 + 授权码     │
       │                    │<────────────────────│
       │                    │ 6. 用授权码换 Token   │
       │                    │────────────────────>│
       │                    │ 7. 返回 access_token│
       │                    │<────────────────────│
       │                    │ 8. 用 Token 获取用户信息│
       │                    │────────────────────>│
       │                    │ 9. 返回用户资料       │
       │                    │<────────────────────│
       │ 10. 重定向到首页    │                    │
       │<───────────────────│                    │
```

### 8.3 GitHub OAuth2 实现

```python
from flask import Flask, request, redirect, url_for, session
import requests

app: Flask = Flask(__name__)
app.config["SECRET_KEY"] = "your-secret"
app.config["GITHUB_CLIENT_ID"] = "your-client-id"
app.config["GITHUB_CLIENT_SECRET"] = "your-client-secret"

@app.route("/login/github")
def github_login() -> str:
    """重定向到 GitHub 授权页面"""
    return redirect(
        "https://github.com/login/oauth/authorize?"
        f"client_id={app.config['GITHUB_CLIENT_ID']}"
        f"&redirect_uri={url_for('github_callback', _external=True)}"
        "&scope=user:email"
    )

@app.route("/login/github/callback")
def github_callback() -> str | tuple:
    """GitHub 回调处理"""
    code = request.args.get("code")
    if not code:
        return "Authorization failed", 400

    token_response = requests.post(
        "https://github.com/login/oauth/access_token",
        headers={"Accept": "application/json"},
        data={
            "client_id": app.config["GITHUB_CLIENT_ID"],
            "client_secret": app.config["GITHUB_CLIENT_SECRET"],
            "code": code,
        }
    )
    access_token = token_response.json().get("access_token")

    user_response = requests.get(
        "https://api.github.com/user",
        headers={"Authorization": f"Bearer {access_token}"}
    )
    github_user = user_response.json()

    user = User.query.filter_by(github_id=github_user["id"]).first()
    if not user:
        user = User(
            username=github_user["login"],
            email=github_user.get("email", f"{github_user['login']}@github"),
            github_id=github_user["id"],
            avatar_url=github_user["avatar_url"],
        )
        db.session.add(user)
        db.session.commit()

    login_user(user)
    return redirect(url_for("index"))
```

### 8.4 OIDC（OpenID Connect）简介

OIDC 是 OAuth2 之上的身份认证层，在返回 access_token 的同时返回 id_token（JWT 格式，包含用户身份信息）：

```python
# OIDC id_token 示例结构
# {
#   "iss": "https://accounts.google.com",        # 签发者
#   "sub": "110169484474386276334",              # 用户唯一标识
#   "aud": "your-client-id.apps.googleusercontent.com",
#   "iat": 1353601026,                           # 签发时间
#   "exp": 1353604926,                           # 过期时间
#   "email": "user@example.com",
#   "email_verified": true,
#   "name": "John Doe",
#   "picture": "https://lh3.googleusercontent.com/.../photo.jpg"
# }

# 使用 authlib 简化 OIDC 集成
# pip install authlib
```

---

## 第九部分：多级权限装饰器系统

### 9.1 实际场景

应用需要细粒度的权限控制：viewer 能读，editor 能读写，admin 能删除，还需要模块级权限（用户管理、内容管理等）。

**问题：如何实现灵活的、多级权限检查？**

### 9.2 权限位掩码（Bitmask）模式

```python
from enum import IntFlag
from functools import wraps
from flask import abort
from flask_login import current_user
from typing import Callable, Any

class Permission(IntFlag):
    """权限位掩码：每个权限占一个二进制位"""
    NONE     = 0
    READ     = 1     # 0001
    WRITE    = 2     # 0010
    DELETE   = 4     # 0100
    ADMIN    = 8     # 1000
    MODERATE = 16
    ALL      = READ | WRITE | DELETE | ADMIN | MODERATE

class Role:
    ADMIN = Permission.ALL
    EDITOR = Permission.READ | Permission.WRITE
    MODERATOR = Permission.READ | Permission.MODERATE
    VIEWER = Permission.READ

def require_permission(permission: Permission) -> Callable:
    """权限检查装饰器工厂"""
    def decorator(f: Callable) -> Callable:
        @wraps(f)
        def wrapper(*args: Any, **kwargs: Any) -> Any:
            if not current_user.is_authenticated:
                abort(401)
            user_permission = current_user.permissions
            if not (user_permission & permission):
                abort(403)
            return f(*args, **kwargs)
        return wrapper
    return decorator

@app.route("/api/users/<int:user_id>", methods=["DELETE"])
@require_permission(Permission.DELETE)
def delete_user(user_id: int) -> dict:
    pass
```

### 9.3 基于资源的权限检查

```python
class ResourcePermission:
    """资源级权限：检查用户是否拥有对特定资源的操作权限"""

    @staticmethod
    def can_edit_post(user: User, post: Post) -> bool:
        """作者或管理员可以编辑"""
        return user.id == post.author_id or user.role == "admin"

    @staticmethod
    def can_delete_comment(user: User, comment: Comment) -> bool:
        """评论作者、文章作者或管理员可以删除"""
        return (
            user.id == comment.user_id
            or user.id == comment.post.author_id
            or user.role == "admin"
        )

def require_resource_owner(
    check_fn,
    resource_loader,
) -> Callable:
    def decorator(f: Callable) -> Callable:
        @wraps(f)
        def wrapper(*args: Any, **kwargs: Any) -> Any:
            if not current_user.is_authenticated:
                abort(401)
            resource = resource_loader(kwargs)
            if not check_fn(current_user, resource):
                abort(403)
            return f(*args, **kwargs)
        return wrapper
    return decorator
```

---

## 第十部分：认证端点速率限制

### 10.1 实际场景

登录接口被暴力破解攻击，注册接口被刷。

**问题：如何限制认证端点的请求频率？**

### 10.2 基于 Redis 的速率限制

```python
import time
from functools import wraps
from flask import request, jsonify
import redis

r = redis.from_url("redis://localhost:6379")

def rate_limit(max_requests: int, window_seconds: int) -> Callable:
    """
    速率限制装饰器
    max_requests: 时间窗口内允许的最大请求数
    window_seconds: 时间窗口大小（秒）
    """
    def decorator(f: Callable) -> Callable:
        @wraps(f)
        def wrapper(*args, **kwargs):
            key = f"rate_limit:{request.remote_addr}:{request.endpoint}"
            current = r.get(key)

            if current and int(current) >= max_requests:
                ttl = r.ttl(key)
                return jsonify({
                    "error": "Too many requests",
                    "retry_after": ttl
                }), 429

            pipe = r.pipeline()
            pipe.incr(key)
            pipe.expire(key, window_seconds)
            pipe.execute()

            return f(*args, **kwargs)
        return wrapper
    return decorator

@app.route("/login", methods=["POST"])
@rate_limit(max_requests=5, window_seconds=60)  # 每分钟最多 5 次
def login():
    ...

@app.route("/register", methods=["POST"])
@rate_limit(max_requests=3, window_seconds=3600)  # 每小时最多 3 次
def register():
    ...
```

### 10.3 渐进式限流策略

```python
def progressive_rate_limit(
    max_requests: int,
    window_seconds: int,
) -> Callable:
    """
    渐进式限流：失败次数越多，等待时间越长
    login 场景特别有用 — 防止暴力破解
    """
    def decorator(f: Callable) -> Callable:
        @wraps(f)
        def wrapper(*args, **kwargs):
            ip = request.remote_addr
            key_fail = f"login_fail:{ip}"

            fail_count = int(r.get(key_fail) or 0)

            if fail_count >= 3:
                penalty = min(2 ** (fail_count - 3) * 60, 3600)
                return jsonify({
                    "error": "Too many failed attempts",
                    "retry_after": penalty
                }), 429

            return f(*args, **kwargs)
        return wrapper
    return decorator
```

---

## 第十一部分：密码哈希算法对比

### 11.1 bcrypt vs argon2 vs scrypt vs PBKDF2

```python
# 算法对比
import time
import bcrypt
import hashlib

def benchmark_hashing(password: str = "test_password", rounds: int = 12) -> None:
    """对比不同哈希算法的性能"""

    start = time.perf_counter()
    bcrypt_hash = bcrypt.hashpw(password.encode(), bcrypt.gensalt(rounds=rounds))
    bcrypt_time = time.perf_counter() - start
    bcrypt_verify = bcrypt.checkpw(password.encode(), bcrypt_hash)

    start = time.perf_counter()
    salt = b"random_salt_16bytes"
    pbkdf2_hash: bytes = hashlib.pbkdf2_hmac(
        "sha256", password.encode(), salt, 600000
    )
    pbkdf2_time = time.perf_counter() - start

    print(f"bcrypt (rounds={rounds}):  {bcrypt_time:.4f}s, verify={'OK' if bcrypt_verify else 'FAIL'}")
    print(f"PBKDF2 (600k):             {pbkdf2_time:.4f}s")

# 输出示例（M1 Mac）:
# bcrypt (rounds=12):  0.3052s, verify=OK
# PBKDF2 (600k):       0.2198s
```

| 算法 | 内存抵抗 | 并行抵抗 | 密码安全 | Flask 支持 | 推荐度 |
|------|---------|---------|---------|-----------|--------|
| bcrypt | 低 | 中 | 高 | Flask-Bcrypt | 最常用，生态成熟 |
| argon2id | 高 | 高 | 最高 | argon2-cffi | 最安全（OWASP 首选） |
| scrypt | 高 | 低 | 高 | hashlib | 中等 |
| PBKDF2 | 低 | 低 | 高 | hashlib | 传统选择（NIST 标准） |

```
OWASP 2024 推荐顺序：argon2id > bcrypt > scrypt > PBKDF2

选择指南：
┌────────────────────────────────────────────────────────────┐
│  场景                    推荐算法        原因               │
│  ────────────────────────────────────────────────────────  │
│  新项目，追求最高安全     argon2id        ASIC/GPU 攻击抵御  │
│  现有项目，生态兼容       bcrypt          库生态最成熟       │
│  嵌入式/低性能设备       PBKDF2          计算开销最小       │
│  合规需求（NIST/FIPS）   PBKDF2          NIST 标准         │
│  高并发登录              bcrypt(rounds=10) 平衡安全和性能   │
└────────────────────────────────────────────────────────────┘
```

### 11.2 Argon2 在 Flask 中的使用

```python
# pip install argon2-cffi
from argon2 import PasswordHasher
from argon2.exceptions import VerifyMismatchError

ph = PasswordHasher(
    time_cost=2,         # 迭代次数（越大越安全但越慢）
    memory_cost=102400,  # 内存消耗（KB），抵抗 GPU 暴力破解
    parallelism=8,       # 并行度
    hash_len=32,
    salt_len=16,
)

class User(db.Model):
    password_hash: str = db.Column(db.String(200))

    def set_password(self, password: str) -> None:
        self.password_hash = ph.hash(password)

    def check_password(self, password: str) -> bool:
        try:
            return ph.verify(self.password_hash, password)
        except VerifyMismatchError:
            return False

    def needs_rehash(self) -> bool:
        """检查是否需要重新哈希（参数变更时）"""
        return ph.check_needs_rehash(self.password_hash)
```

---

## 第十二部分：常见坑点排查

### 12.1 JWT 密钥泄露导致 Token 可被伪造

**错误信息：**

无明显错误！攻击者伪造的 Token 被正常接受。

**根因分析：**

JWT 签名密钥（JWT_SECRET_KEY）使用弱密钥或默认值，攻击者猜到密钥后可以伪造任意 Token。

**修复：**

```python
import secrets

# ❌ 错误：使用写死在代码中的弱密钥
app.config["JWT_SECRET_KEY"] = "my-jwt-secret"

# ✅ 正确：使用环境变量 + 强随机密钥
app.config["JWT_SECRET_KEY"] = os.environ.get(
    "JWT_SECRET_KEY",
    secrets.token_urlsafe(32)
)
```

### 12.2 密码存储忘加盐（或盐值固定）

**错误信息：**

（无运行时错误）但攻击者使用彩虹表可在秒级破解所有用户密码。

**对比：**

```python
import hashlib

# ❌ 错误一：无盐
password = "123456"
weak_hash = hashlib.sha256(password.encode()).hexdigest()
# 固定输出: 8d969eef6ecad3c29a3a629280e686cf0c3f5d5a86aff3ca12020c923adc6c92
# 彩虹表一击即中

# ❌ 错误二：固定盐
salt = "my-app-salt"  # 代码中写死 = 无盐
hash2 = hashlib.sha256((password + salt).encode()).hexdigest()

# ✅ 正确：bcrypt（自动生成随机盐，包含在哈希结果中）
import bcrypt
hashed = bcrypt.hashpw(password.encode(), bcrypt.gensalt())
# $2b$12$LJ3m4ys3Lk0TSwZg5kYpUeYXV5xX5SZZN8nSzr7VOWLXcLXhM3ZZ2
#           ^^^^^^^^^^^^^^^^^^^^^^^^
#           盐值嵌入在哈希结果中
```

### 12.3 CSRF Token Missing

**错误信息：**

```
400 Bad Request: The CSRF token is missing.
```

**常见原因：**

1. 表单中忘记添加 `{{ csrf_token() }}`
2. AJAX 请求忘记带 X-CSRFToken 头
3. 使用 Flask-WTF 但未初始化 CSRF

**预防：**

```html
<!-- 模板中加入 -->
<input type="hidden" name="csrf_token" value="{{ csrf_token() }}">

<!-- AJAX 请求 -->
<script>
fetch("/api/data", {
    method: "POST",
    headers: {
        "Content-Type": "application/json",
        "X-CSRFToken": "{{ csrf_token() }}"
    },
    body: JSON.stringify(data)
})
</script>
```

### 12.4 问题排查表

| 现象 | 可能原因 | 检查方法 | 解决方案 |
|------|---------|---------|---------|
| 登录后仍 302 重定向到登录页 | next 参数问题或 session 不完整 | 检查 session 内容 | 确保 `login_user(user)` 调用成功 |
| JWT Token 立即过期 | 时区/时间不同步 | 检查服务器时间 `date -u` | NTP 同步时间，或增加 clock skew |
| current_user 为空 | user_loader 未注册 | 检查 `@login_manager.user_loader` | 在应用初始化时注册回调 |
| bcrypt 验证总是失败 | 编码问题 | 检查 str vs bytes | `decode()` 存储，`encode()` 验证 |
| 401 但 Token 未过期 | Token 格式错误 | 检查 Authorization 头格式 | `Bearer <token>` 注意空格 |
| OAuth2 回调 code 为空 | redirect_uri 不匹配 | 比较请求中的 uri 和注册的 | 确保完全一致（含协议、端口、路径） |

---

## 第十三部分：调试与排错实战

### 13.1 调试会话：JWT 验证失败

```
场景：客户端带上 Token 访问 /api/profile，返回 401 Unauthorized。

调试步骤：

Step 1: 手动解码 Token
$ echo "eyJhbGciOiJIUzI1NiIs..." | cut -d'.' -f2 | base64 -d 2>/dev/null | python -m json.tool
{
    "sub": "1",
    "iat": 1699000000,
    "exp": 1698996400,  ← 注意：exp < iat！过期时间早于签发时间
    "type": "access"
}
结论：Token 已过期，可能是服务器时间设置错误。

Step 2: 检查服务器时间
$ date -u
Thu Nov  2 10:30:00 UTC 2023

Step 3: 检查 JWT 配置
在代码中打印过期时间：
from datetime import timedelta
expires = app.config.get("JWT_ACCESS_TOKEN_EXPIRES")
print(f"Token 过期设置: {expires}")

Step 4: 临时 debug 模式
@app.route("/debug-token")
@jwt_required()
def debug_token():
    from flask_jwt_extended import get_jwt
    claims = get_jwt()
    return {"claims": claims}
```

### 13.2 调试会话：用户登录后仍然被重定向

```
场景：用户输入正确密码，重定向到首页，但显示未登录状态。

Step 1: 添加调试中间件
@app.before_request
def debug_auth() -> None:
    from flask import session
    from flask_login import current_user
    print(f"[AUTH DEBUG] path={request.path}")
    print(f"  session: {dict(session)}")
    print(f"  current_user: {current_user}")
    print(f"  is_auth: {current_user.is_authenticated}")

Step 2: 观察输出
[AUTH DEBUG] path=/login
  session: {'_flashes': [], '_fresh': False, '_id': 'abc123'}
[AUTH DEBUG] path=/dashboard
  session: {}  ← session 为空！Cookie 可能在重定向中丢失

Step 3: 检查 Cookie 配置
In [1]: app.config['SESSION_COOKIE_DOMAIN']
Out[1]: '.example.com'  ← 当前请求域名是 localhost，不匹配

修复：开发环境设置 SESSION_COOKIE_DOMAIN = None
```

---

## 第十四部分：进阶安全加固

### 14.1 账户锁定策略

```python
# 防暴力破解：连续失败 N 次后锁定账户
FAILED_ATTEMPTS_LIMIT = 5
from datetime import timedelta
LOCKOUT_DURATION = timedelta(minutes=15)

@app.route("/login", methods=["POST"])
def login_with_lockout():
    username = request.form.get("username", "")
    password = request.form.get("password", "")

    user = User.query.filter_by(username=username).first()

    if user and user.locked_until and user.locked_until > datetime.utcnow():
        remaining = (user.locked_until - datetime.utcnow()).seconds
        return {"error": f"Account locked. Try again in {remaining}s"}, 423

    if user and user.check_password(password):
        user.failed_attempts = 0
        user.locked_until = None
        db.session.commit()
        login_user(user)
        return redirect(url_for("dashboard"))

    if user:
        user.failed_attempts += 1
        if user.failed_attempts >= FAILED_ATTEMPTS_LIMIT:
            user.locked_until = datetime.utcnow() + LOCKOUT_DURATION
        db.session.commit()

    return {"error": "Invalid credentials"}, 401
```

### 14.2 会话固定攻击防范

```python
# 登录后重新生成 session ID（防止 Session Fixation）
from flask import session
from flask_login import login_user

@app.route("/login", methods=["POST"])
def login_secure():
    user = authenticate_user(request.form)
    if user:
        # 登录前清除旧 session（防止 session fixation）
        session.clear()
        login_user(user)
        # 标记 session 为新鲜（刚验证过）
        session["_fresh"] = True
    return redirect(url_for("index"))
```

| 进阶知识点 | 说明 |
|-------------|------|
| 服务端 Session | Redis 存储 Session，突破 4KB 限制 |
| Token 轮换 | 刷新时同时更换 refresh token 防重放 |
| Token 黑名单 | 撤销 Token 时加入黑名单，实时生效 |
| OAuth2 授权码 | 标准第三方登录流程 |
| 权限位掩码 | IntFlag 实现灵活的多权限组合 |
| 速率限制 | 防暴力破解，渐进式惩罚 |
| Argon2 | OWASP 首选密码哈希算法 |
| 账户锁定 | 连续失败自动锁定 |