# 11-测试与部署

> Python 3.11+

本章讲解 Flask 应用测试和生产部署。

---

## 概念铺垫

Flask 提供 `test_client` 用于模拟 HTTP 请求测试，pytest 通过 fixture 作用域链（session → module → class → function）管理测试资源的创建和销毁。生产部署使用 Gunicorn（WSGI 服务器），支持多种 Worker 模型：sync（CPU 密集）、gthread（多线程混合）、gevent/eventlet（协程 I/O 密集）。Docker 多阶段构建分离构建环境与运行环境，显著减小镜像体积。

---

### L1 理解层：会用

## 第一部分：Flask 测试客户端

### 1.1 实际场景

你需要验证 API 是否正常工作，测试登录功能、注册功能等。

**问题：如何编写自动化测试验证应用功能？**

### 1.2 基本用法

Flask 提供了测试客户端，可以模拟请求测试应用。

```python
import pytest
from flask import Flask
from flask.testing import FlaskClient


@pytest.fixture
def app() -> Flask:
    app: Flask = create_app("testing")
    with app.app_context():
        yield app


@pytest.fixture
def client(app: Flask) -> FlaskClient:
    return app.test_client()


def test_index(client: FlaskClient) -> None:
    """测试首页"""
    response = client.get("/")
    assert response.status_code == 200


def test_login(client: FlaskClient) -> None:
    """测试登录"""
    response = client.post("/auth/login", data={
        "username": "admin",
        "password": "admin123"
    })
    assert response.status_code == 302  # 重定向
```

### 1.3 测试 JSON API

```python
from typing import Any

def test_api_create_user(client: FlaskClient) -> None:
    """测试创建用户 API"""
    response = client.post("/api/users", json={
        "username": "testuser",
        "email": "test@example.com"
    })
    assert response.status_code == 201
    data: dict[str, Any] = response.get_json()
    assert data["username"] == "testuser"
```

---

## 第二部分：pytest 配置

### 2.1 实际场景

测试需要数据库初始化、配置加载等准备工作。

**问题：如何配置测试环境和 fixtures？**

### 2.2 pytest.ini

```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
```

### 2.3 conftest.py

```python
import pytest
from flask import Flask
from flask.testing import FlaskClient, FlaskCliRunner
from app import create_app, db


@pytest.fixture(scope="session")
def app() -> Flask:
    app: Flask = create_app("testing")
    with app.app_context():
        db.create_all()
        yield app
        db.drop_all()


@pytest.fixture
def client(app: Flask) -> FlaskClient:
    return app.test_client()


@pytest.fixture
def runner(app: Flask) -> FlaskCliRunner:
    return app.test_cli_runner()
```

---

## 第三部分：生产部署

### 3.1 实际场景

开发完成后，需要将应用部署到生产服务器。

**问题：如何部署 Flask 应用到生产环境？**

### 3.2 Gunicorn

```bash
# 安装
uv add gunicorn

# 运行
uv run gunicorn -w 4 -b 0.0.0.0:8000 app:app
```

### 3.3 Docker 部署

**Dockerfile：**

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:8000", "app:app"]
```

**docker-compose.yml：**

```yaml
version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://user:pass@db:5432/app
    depends_on:
      - db
      - redis

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: app
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine

volumes:
  postgres_data:
```

### 3.4 Nginx 配置

```nginx
server {
    listen 80;
    server_name example.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /static {
        alias /app/static;
    }
}
```

---

### L2 实践层：用好

| 做法 | 原因 | 示例 |
|------|------|------|
| session 级 fixture 复用昂贵资源 | 数据库连接只创建一次 | `@pytest.fixture(scope="session")` |
| 每个测试独立事务 | 测试间数据不污染 | `db.session.rollback()` |
| `client.get_json()` 验证 JSON 响应 | 比手动解析更安全 | `data = response.get_json()` |
| Worker 数量公式 `2*cpu+1` | 平衡 CPU 利用率 | `gunicorn -w 5 app:app` |
| 多阶段 Docker 构建 | 减小镜像体积 | `FROM ... as builder` |

#### 反模式

```python
# ❌ 错误：测试共享可变数据
users = [
    User(name="test")  # 多个测试修改同一列表
]
def test_a(): users[0].name = "modified"
def test_b(): assert users[0].name == "test"  # 可能失败

# ❌ 错误：生产仍用 flask run
flask run  # 开发服务器，单进程

# ✅ 正确：用 Gunicorn
gunicorn -w 4 -b 0.0.0.0:8000 app:app
```

#### Worker 模型选择

| 应用特征 | 推荐 Worker |
|---------|------------|
| CPU 密集型 | sync |
| 中等 I/O 等待 | gthread |
| 高 I/O、长连接 | gevent |

---

### L3 专家层：深入

## 第四部分：L3 专家层

### 6.1 pytest fixture 的作用域链

pytest fixture 通过作用域控制资源的创建和销毁时机，理解作用域链对测试性能和隔离性至关重要。

#### 作用域层级

```
+---------------------------------------------------------------+
|                   Fixture 作用域链                              |
+---------------------------------------------------------------+
|                                                               |
|  session (整个测试会话只创建一次)                               |
|  └── package (每个 Python 包创建一次)                           |
|      └── module (每个测试文件创建一次)                           |
|          └── class (每个测试类创建一次)                          |
|              └── function (每个测试函数创建一次，默认)            |
|                                                               |
|  外层 fixture 可被内层 fixture 依赖                              |
|  内层 fixture 不能依赖外层以外的 fixture                         |
|                                                               |
+---------------------------------------------------------------+
```

```python
import pytest
from flask import Flask
from flask.testing import FlaskClient
from typing import Generator

# session 级：整个测试会话共享
# 适用：创建数据库引擎、加载配置
@pytest.fixture(scope="session")
def app_factory() -> Flask:
    print("session setup: 创建应用实例")
    app: Flask = create_app("testing")
    yield app
    print("session teardown: 清理应用")

# module 级：每个测试文件共享
# 适用：创建测试数据库表
@pytest.fixture(scope="module")
def db(app_factory: Flask) -> Generator[None, None, None]:
    print("module setup: 创建数据库表")
    with app_factory.app_context():
        db.create_all()
        yield
        db.drop_all()
    print("module teardown: 删除数据库表")

# function 级（默认）：每个测试独立
# 适用：隔离测试数据
@pytest.fixture
def client(app_factory: Flask, db: None) -> FlaskClient:
    return app_factory.test_client()


# 依赖链：client → db → app_factory
# 执行顺序：
#   1. session: app_factory setup
#   2. module:  db setup
#   3. function: client setup
#   4. 运行测试
#   5. function: client teardown
#   6. module:  db teardown
#   7. session: app_factory teardown
```

#### 性能考量

| 作用域 | 创建次数 | 隔离性 | 性能 | 适用场景 |
|-------|---------|-------|-----|---------|
| session | 1 次 | 无 | 最好 | 配置加载、数据库连接池 |
| module | 每个文件 1 次 | 文件级 | 好 | 数据库表结构、共享数据 |
| class | 每个类 1 次 | 类级 | 中 | 同一业务逻辑的多个测试 |
| function | 每个测试 1 次 | 完全隔离 | 最差 | 状态敏感的操作（认证、数据变更） |

#### 设计动机

| 设计 | 解决的问题 |
|-----|-----------|
| 作用域分层 | 平衡测试隔离性与执行速度 |
| 外层可被内层依赖 | 资源复用：DB 引擎只创建一次，每次测试复用连接 |
| 自动 teardown | 防止测试间状态泄漏（脏数据） |
| `scope="function"` 默认 | 安全优先：新开发者写测试天然隔离，不易踩坑 |

### 6.2 Gunicorn 的 Worker 模型

Gunicorn 支持多种 Worker 类型，选择取决于应用的 I/O 特征。

#### Worker 架构

```
+-----------------------------------------------------------------+
|                      Gunicorn Master Process                     |
|                                                                  |
|  ┌────────────────────────────────────────────────────────────┐  |
|  │                    Master (管理进程)                        │  |
|  │  - 监听信号 (HUP/TERM/USR2)                                 │  |
|  │  - 监控 Worker 健康                                         │  |
|  │  - 动态扩缩容                                               │  |
|  └────────────────────────┬──────────────────────────────────┘  |
|                           │ fork()                              |
|          ┌────────────────┼────────────────┐                    |
|          ▼                ▼                ▼                    |
|  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          |
|  │  Worker 1     │  │  Worker 2     │  │  Worker N     │          |
|  │  ┌──────────┐ │  │  ┌──────────┐ │  │  ┌──────────┐ │          |
|  │  │ Thread 1 │ │  │  │ Thread 1 │ │  │  │ Thread 1 │ │          |
|  │  │ Thread 2 │ │  │  │ Thread 2 │ │  │  │ Thread 2 │ │          |
|  │  │ ...      │ │  │  │ ...      │ │  │  │ ...      │ │          |
|  │  └──────────┘ │  │  └──────────┘ │  │  └──────────┘ │          |
|  │  (gthread)    │  │  (gthread)    │  │  (gthread)    │          |
|  └──────────────┘  └──────────────┘  └──────────────┘          |
+-----------------------------------------------------------------+
```

#### Worker 类型对比

```python
# sync: 同步 Worker（默认）
# 每个 Worker 同时只能处理一个请求
# gunicorn -w 4 -k sync app:app
#
# 适用：CPU 密集型、无外部 I/O 等待
# 公式：workers = 2 * CPU核心数 + 1

# gthread: 多线程 Worker
# 每个 Worker 有线程池，可同时处理多个请求
# gunicorn -w 4 -k gthread --threads 4 app:app
#
# 适用：少量 I/O 等待的混合场景

# eventlet / gevent: 协程 Worker
# 每个 Worker 使用协程（greenlet），可并发处理数百请求
# gunicorn -w 4 -k gevent --worker-connections 1000 app:app
#
# 适用：大量 I/O 等待（数据库查询、HTTP 调用）
```

#### 性能考量

| Worker 类型 | 并发能力 | 内存开销 | 上下文切换 | 适用场景 |
|-----------|---------|---------|-----------|---------|
| sync | 1 req/worker | 最低 | 进程级 | CPU 密集型、简单 API |
| gthread | threads/worker | 低 | 线程级 | 中等 I/O、通用场景 |
| gevent | 数百 req/worker | 中 | 协程级 | 高 I/O、长连接 |
| eventlet | 数百 req/worker | 中 | 协程级 | 高 I/O、WebSocket |

#### 设计动机

| 设计 | 解决的问题 |
|-----|-----------|
| 多 Worker 进程 | 利用多核 CPU，一个崩溃不影响其他 |
| Master-Worker 架构 | Master 负责管理，Worker 专注请求处理 |
| 协程 Worker | 解决同步 Worker 在 I/O 等待时浪费 CPU 的问题 |
| `2 * cores + 1` 公式 | 平衡 CPU 利用率与上下文切换开销 |

### 6.3 Docker 多阶段构建原理

多阶段构建将构建环境和运行环境分离，显著减小最终镜像体积。

#### 构建流程

```
+---------------------------------------------------------------+
|                    Docker 多阶段构建                             |
+---------------------------------------------------------------+
|                                                               |
|  Stage 1: builder (构建阶段)                                   |
|  ┌──────────────────────────────────────────────────────┐     |
|  │  FROM python:3.11 as builder                          │     |
|  │  # 包含编译工具链、头文件等                            │     |
|  │  COPY requirements.txt .                              │     |
|  │  RUN pip install --no-cache-dir -r requirements.txt   │     |
|  │  COPY . .                                             │     |
|  │  # 可能包含：编译 C 扩展、下载依赖、运行测试            │     |
|  └──────────────────────┬───────────────────────────────┘     |
|                         │ 仅复制产物                            |
|                         ▼                                     |
|  Stage 2: production (运行阶段)                                |
|  ┌──────────────────────────────────────────────────────┐     |
|  │  FROM python:3.11-slim as production                  │     |
|  │  # 精简基础镜像，无编译工具                            │     |
|  │  COPY --from=builder /usr/local/lib/python3.11/ \     │     |
|  │                   /usr/local/lib/python3.11/          │     |
|  │  COPY --from=builder /app /app                        │     |
|  │  WORKDIR /app                                         │     |
|  │  CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:8000",   │     |
|  │        "app:app"]                                     │     |
|  └──────────────────────────────────────────────────────┘     |
|                                                               |
|  最终镜像只包含 Stage 2 的内容                                  |
|  Stage 1 的所有中间层都不会出现在最终镜像中                      |
+---------------------------------------------------------------+
```

```dockerfile
# 完整多阶段构建示例
FROM python:3.11 as builder

WORKDIR /build
COPY requirements.txt .
# 安装到指定目录，方便后续复制
RUN pip install --prefix=/install --no-cache-dir -r requirements.txt

# 可选：运行测试
COPY . .
RUN python -m pytest tests/ -q

# 最终镜像
FROM python:3.11-slim

WORKDIR /app
# 只复制已安装的依赖和源代码
COPY --from=builder /install /usr/local
COPY --from=builder /build /app

EXPOSE 8000
CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:8000", "app:app"]
```

#### 性能考量

| 策略 | 镜像大小 | 构建时间 | 安全性 |
|-----|---------|---------|-------|
| 单阶段（完整） | 大（~900MB） | 短 | 差（含工具链和缓存） |
| 多阶段（slim） | 中（~200MB） | 中 | 好（无编译工具） |
| 多阶段（alpine） | 小（~50MB） | 长 | 最好（最小攻击面） |

#### 设计动机

| 设计 | 解决的问题 |
|-----|-----------|
| 多阶段分离 | 构建依赖（gcc、make）不应出现在生产镜像中 |
| slim/alpine 基础镜像 | 减小体积 = 更快部署 + 更少漏洞面 |
| `--no-cache-dir` | 不缓存 pip 下载，减小中间层 |
| 精确 COPY | 只复制产物，不复制 .git、tests、docs |

### 6.4 知识关联

```
+---------------------+          +------------------------+          +--------------------+
| pytest Fixture      |          | Gunicorn Worker        |          | Docker 多阶段       |
| 作用域链            |          | 模型                   |          | 构建               |
+---------------------+          +------------------------+          +--------------------+
|  session: 全局共享  |          |  sync: CPU 密集型       |          |  Builder: 完整工具  |
|  module: 文件级     |--------->|  gthread: 混合型        |--------->|  Production: 精简   |
|  function: 测试级   |          |  gevent: I/O 密集型     |          |  COPY --from       |
|  自动 teardown      |          |  Master-Worker          |          |  安全隔离           |
+---------------------+          +------------------------+          +--------------------+
         |                                 |                                 |
         v                                 v                                 v
+---------------------------------------------------------------------------+
|                          设计目标                                          |
|  测试隔离 · 执行效率 · 并发能力 · 资源利用 · 安全部署 · 可维护性            |
+---------------------------------------------------------------------------+
```

---

## 总结

| 知识点 | 说明 |
|---------|------|
| test_client | Flask 测试客户端 |
| pytest | 测试框架 |
| fixtures | 测试准备 |
| Gunicorn | WSGI 服务器 |
| Docker | 容器化部署 |
| Nginx | 反向代理 |

---

## 第五部分：测试进阶 — 临时数据库与事务隔离

### 5.1 实际场景

每个测试需要独立的数据库状态，避免测试间数据污染。

**问题：如何在测试中使用临时数据库并保证测试隔离？**

### 5.2 内存 SQLite + 事务回滚

```python
# tests/conftest.py
import pytest
from flask import Flask
from flask.testing import FlaskClient
from app import create_app, db as _db

@pytest.fixture(scope="session")
def app() -> Flask:
    """创建测试应用（session 级，整个测试会话共享）"""
    app: Flask = create_app("testing")

    with app.app_context():
        _db.create_all()
        yield app
        _db.drop_all()


@pytest.fixture
def db_session(app: Flask):
    """
    每个测试函数独立的数据库事务
    测试结束后自动回滚，不污染数据库
    """
    connection = _db.engine.connect()
    transaction = connection.begin()
    _db.session = _db.create_scoped_session(
        options={"bind": connection, "binds": {}}
    )

    yield _db.session

    transaction.rollback()
    connection.close()
    _db.session.remove()


@pytest.fixture
def client(app: Flask, db_session) -> FlaskClient:
    return app.test_client()


# 测试中使用 fixture
def test_create_user(db_session, client: FlaskClient) -> None:
    response = client.post("/api/users", json={
        "username": "test_user",
        "email": "test@example.com",
    })
    assert response.status_code == 201

    # 验证数据库
    user = User.query.filter_by(username="test_user").first()
    assert user is not None
    assert user.email == "test@example.com"
    # 测试结束后事务自动回滚，不影响其他测试
```

### 5.3 测试数据工厂

```python
# tests/factories.py
import factory
import factory.fuzzy
from app import db
from app.models import User, Article
from datetime import datetime

class UserFactory(factory.alchemy.SQLAlchemyModelFactory):
    class Meta:
        model = User
        sqlalchemy_session = db.session
        sqlalchemy_session_persistence = "commit"

    username = factory.Faker("user_name")
    email = factory.Faker("email")
    password_hash = "$2b$12$LJ3m4ys3Lk0TSwZg5kYpUeYXV5xX5SZZN8nSzr7VOWLXcLXhM3ZZ2"
    role = "user"
    is_active = True
    created_at = factory.LazyFunction(datetime.utcnow)


class ArticleFactory(factory.alchemy.SQLAlchemyModelFactory):
    class Meta:
        model = Article
        sqlalchemy_session = db.session
        sqlalchemy_session_persistence = "commit"

    title = factory.Faker("sentence", nb_words=4)
    content = factory.Faker("text", max_nb_chars=500)
    author = factory.SubFactory(UserFactory)
    published = True
    created_at = factory.LazyFunction(datetime.utcnow)


# 使用
def test_article_list_with_factory(db_session, client: FlaskClient) -> None:
    # 快速创建 10 篇测试文章
    author = UserFactory(username="test_author")
    articles = ArticleFactory.create_batch(10, author=author)

    response = client.get("/api/articles")
    assert response.status_code == 200
    data = response.get_json()
    assert len(data["articles"]) == 10
```

---

## 第六部分：Mock 外部服务

### 6.1 实际场景

测试涉及发送邮件、调用第三方 API 等外部依赖，需要在测试中隔离。

**问题：如何 Mock 外部服务避免测试依赖外部系统？**

### 6.2 requests-mock 示例

```python
# pip install requests-mock pytest-mock

import requests_mock
from unittest.mock import patch, MagicMock

def test_github_oauth_callback(client: FlaskClient, requests_mock) -> None:
    """Mock GitHub OAuth API 响应"""
    # Mock access_token 接口
    requests_mock.post(
        "https://github.com/login/oauth/access_token",
        json={"access_token": "mock_token_abc123"},
    )

    # Mock 用户信息接口
    requests_mock.get(
        "https://api.github.com/user",
        json={
            "id": 12345,
            "login": "test_user",
            "email": "test@github.com",
            "avatar_url": "https://example.com/avatar.png",
        },
    )

    response = client.get(
        "/login/github/callback?code=mock_code",
        follow_redirects=True
    )

    assert response.status_code == 200
    # 验证用户被创建
    assert User.query.filter_by(github_id=12345).first() is not None
```

### 6.3 Mock Celery 任务

```python
from unittest.mock import patch

def test_send_email_endpoint(client: FlaskClient) -> None:
    """Mock Celery 任务避免实际发送邮件"""
    with patch("app.tasks.send_email.delay") as mock_delay:
        mock_delay.return_value.id = "mock-task-uuid"

        response = client.post("/api/send-email", json={
            "to": "user@example.com",
            "subject": "Test",
            "body": "Hello",
        })

        assert response.status_code == 202
        assert response.get_json()["task_id"] == "mock-task-uuid"
        mock_delay.assert_called_once_with(
            to="user@example.com",
            subject="Test",
            body="Hello",
        )
```

### 6.4 Mock 装饰器最佳实践

```python
# conftest.py — 全局 Mock fixture
import pytest

@pytest.fixture(autouse=True)
def mock_external_services(request):
    """自动 Mock 所有外部服务（根据需要选择性启用）"""
    if "integration" in request.keywords:
        # 集成测试不 mock
        yield
        return

    with patch("app.services.email.send") as mock_email, \
         patch("app.services.payment.PaymentGateway") as mock_payment, \
         patch("redis.Redis.from_url") as mock_redis:

        mock_email.return_value = True
        mock_payment.return_value.charge.return_value = {"status": "ok"}
        mock_redis.return_value.ping.return_value = True

        yield


# 使用 marker 控制
# @pytest.mark.integration
# def test_real_email():
#     ...  # 此测试不会 mock
```

---

## 第七部分：单元测试 vs 集成测试策略

### 7.1 实际场景

需要决定哪些测试用单元测试，哪些用集成测试。

**问题：如何制定 Flask 应用的测试策略？**

### 7.2 测试金字塔

```
        ┌───────┐
       /   E2E    \        少数（5-10%）：全链路测试
      /───────────\         
     /  Integration \      中等（20-30%）：API + DB + 外部服务
    /───────────────\       
   /   Unit Tests    \     大量（60-70%）：函数、类、模型
  └───────────────────┘    
```

### 7.3 Flask 测试分层示例

```python
# ============== 单元测试 ==============
# 测试模型方法，不涉及 Flask context
class TestUserModel:
    def test_set_password(self):
        user = User(username="test", email="test@test.com")
        user.set_password("mypassword")
        assert user.password_hash != "mypassword"
        assert user.check_password("mypassword") is True
        assert user.check_password("wrongpassword") is False

    def test_article_slug(self):
        article = Article(title="Hello World 2024!")
        assert article.generate_slug() == "hello-world-2024"


# ============== 集成测试 ==============
# 测试 API 端点 + 数据库
class TestArticleAPI:
    def test_create_and_get_article(self, db_session, client: FlaskClient):
        # 创建文章
        resp = client.post("/api/articles", json={
            "title": "Test Article",
            "content": "Test content",
        })
        assert resp.status_code == 201
        article_id = resp.get_json()["id"]

        # 获取文章
        resp = client.get(f"/api/articles/{article_id}")
        assert resp.status_code == 200
        assert resp.get_json()["title"] == "Test Article"

    def test_list_articles_pagination(self, db_session, client: FlaskClient):
        author = UserFactory()
        ArticleFactory.create_batch(25, author=author)

        resp = client.get("/api/articles?page=2&per_page=10")
        data = resp.get_json()
        assert len(data["articles"]) == 10  # 第二页应有 10 条
        assert data["total"] == 25


# ============== E2E 测试 ==============
# 模拟完整用户流程
class TestUserJourney:
    def test_complete_signup_and_post(self, client: FlaskClient):
        # 1. 注册
        resp = client.post("/api/register", json={
            "username": "new_user",
            "email": "new@example.com",
            "password": "SecurePass123!",
        })
        assert resp.status_code == 201

        # 2. 登录获取 Token
        resp = client.post("/api/login", json={
            "username": "new_user",
            "password": "SecurePass123!",
        })
        token = resp.get_json()["token"]

        # 3. 创建文章
        resp = client.post("/api/articles",
            json={"title": "First Post", "content": "Hello!"},
            headers={"Authorization": f"Bearer {token}"}
        )
        assert resp.status_code == 201
        article_id = resp.get_json()["id"]

        # 4. 查看文章
        resp = client.get(f"/api/articles/{article_id}")
        assert resp.status_code == 200
```

### 7.4 测试覆盖率配置

```ini
# pyproject.toml
[tool.coverage.run]
source = ["app"]
omit = ["*/tests/*", "*/migrations/*", "app/__init__.py"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
]

# 运行覆盖率
# uv run pytest --cov=app --cov-report=html
# open htmlcov/index.html
```

---

## 第八部分：Gunicorn 生产配置深度

### 8.1 完整 Gunicorn 配置

```python
# gunicorn_config.py
import multiprocessing
import os

# 绑定地址
bind = "0.0.0.0:8000"

# Worker 配置
workers = multiprocessing.cpu_count() * 2 + 1
worker_class = "gthread"
threads = 4
worker_connections = 1000

# 超时控制
timeout = 30                    # Worker 处理请求超时
graceful_timeout = 30           # 优雅关闭超时
keepalive = 2                   # Keep-Alive 连接保持时间

# 进程管理
max_requests = 10000            # 每个 Worker 处理 1 万个请求后重启
max_requests_jitter = 1000      # 随机抖动防同时重启
preload_app = True              # Worker fork 前加载应用

# 日志
accesslog = "/var/log/flask/access.log"
errorlog = "/var/log/flask/error.log"
loglevel = "info"
access_log_format = '%(h)s %(l)s %(u)s %(t)s "%(r)s" %(s)s %(b)s "%(f)s" "%(a)s" %(D)s'

# SSL（如果需要直接使用 HTTPS）
# certfile = "/etc/ssl/certs/server.crt"
# keyfile = "/etc/ssl/private/server.key"

# 自定义 Hook
def when_ready(server) -> None:
    """服务器就绪时执行"""
    print(f"Gunicorn 已就绪，监听 {bind}")

def on_starting(server) -> None:
    """服务器启动前执行"""
    print("Gunicorn 正在启动...")

def worker_int(worker) -> None:
    """Worker 收到 SIGINT"""
    print(f"Worker {worker.pid} 收到中断信号")

def post_fork(server, worker) -> None:
    """Worker fork 后执行"""
    server.log.info(f"Worker {worker.pid} 已 fork")
```

### 8.2 systemd 服务配置

```ini
# /etc/systemd/system/flask-app.service
[Unit]
Description=Flask Application
After=network.target redis.service postgresql.service

[Service]
User=flask
Group=flask
WorkingDirectory=/opt/flask-app
Environment="FLASK_ENV=production"
EnvironmentFile=/opt/flask-app/.env
ExecStart=/opt/flask-app/.venv/bin/gunicorn -c /opt/flask-app/gunicorn_config.py app:app
ExecReload=/bin/kill -s HUP $MAINPID
Restart=always
RestartSec=5
KillMode=mixed
TimeoutStopSec=30

[Install]
WantedBy=multi-user.target
```

```bash
# 启用服务
sudo systemctl daemon-reload
sudo systemctl enable flask-app
sudo systemctl start flask-app
sudo systemctl status flask-app

# 优雅重启（不中断服务）
sudo systemctl reload flask-app
# 或者发送 USR2 信号（无停机升级）
# kill -USR2 $(cat /var/run/gunicorn.pid)
```

### 8.3 Worker 类型选择决策树

```
选择 Worker 类型：
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  应用主要特征是什么？                                        │
│  ├── CPU 密集（计算、加密、图像处理）                         │
│  │   └── sync (每个 Worker 单线程，无 GIL 竞争)              │
│  ├── 中等 I/O（数据库查询、简单 API 调用）                    │
│  │   └── gthread (少量线程 + 多进程，平衡方案)               │
│  └── 高 I/O（长连接、WebSocket、大量外部调用）               │
│      └── gevent (协程，数千并发连接)                         │
│                                                             │
│  特殊场景：                                                  │
│  ├── 使用 C 扩展（pandas, numpy） → 避免 gevent             │
│  ├── 需要 SSE/WebSocket → eventlet                          │
│  └── 不确定 → 从 gthread 开始，监控后调整                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 第九部分：Nginx 反向代理深入

### 9.1 完整 Nginx 配置

```nginx
# /etc/nginx/sites-available/flask-app
upstream flask_backend {
    # 最少连接算法
    least_conn;

    server 127.0.0.1:8000 weight=1 max_fails=3 fail_timeout=30s;
    server 127.0.0.1:8001 weight=1 max_fails=3 fail_timeout=30s;
    server 127.0.0.1:8002 weight=1 max_fails=3 fail_timeout=30s;

    # 健康检查（Nginx Plus 或 nginx-mod-njs）
    # 开源 Nginx 版本可使用 max_fails + fail_timeout 实现简单健康检查
}

# 限流区域定义
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
limit_conn_zone $binary_remote_addr zone=conn_limit:10m;

server {
    listen 80;
    server_name example.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name example.com;

    # SSL 配置
    ssl_certificate     /etc/ssl/certs/server.crt;
    ssl_certificate_key /etc/ssl/private/server.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # 访问日志
    access_log /var/log/nginx/flask-access.log;

    # 客户端请求体大小限制
    client_max_body_size 16M;

    # 静态文件（Nginx 直接返回，不经过 Gunicorn）
    location /static/ {
        alias /opt/flask-app/app/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 上传文件
    location /uploads/ {
        alias /opt/flask-app/uploads/;
        internal;  # 仅内部重定向，不直接暴露
    }

    # API — 带限流
    location /api/ {
        limit_req zone=api_limit burst=20 nodelay;
        limit_conn conn_limit 10;

        proxy_pass http://flask_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时
        proxy_read_timeout 30s;
        proxy_connect_timeout 5s;
        proxy_send_timeout 30s;

        # 缓冲
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 32k;
    }

    # 健康检查端点（不记录日志）
    location /health {
        proxy_pass http://flask_backend;
        access_log off;
        proxy_read_timeout 2s;
    }
}
```

### 9.2 Nginx 性能调优

```nginx
# /etc/nginx/nginx.conf
user nginx;
worker_processes auto;          # 自动匹配 CPU 核心数
worker_rlimit_nofile 65535;     # 每个 Worker 最大文件描述符

events {
    worker_connections 4096;    # 每个 Worker 最大连接数
    use epoll;                  # Linux 高效事件模型
    multi_accept on;            # 同时接受多个连接
}

http {
    # 基础优化
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript
               text/xml application/xml text/javascript image/svg+xml;
    gzip_disable "MSIE [1-6]\.";

    # 缓冲区
    client_body_buffer_size 128k;
    client_header_buffer_size 4k;
    large_client_header_buffers 4 16k;

    include /etc/nginx/conf.d/*.conf;
    include /etc/nginx/sites-enabled/*;
}
```

---

## 第十部分：Docker 多阶段构建深度

### 10.1 完整生产 Dockerfile

```dockerfile
# Dockerfile
# ───── Stage 1: Builder ─────
FROM python:3.11-slim-bookworm AS builder

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential gcc && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /build

COPY requirements.txt .
RUN pip install --prefix=/install --no-cache-dir \
    -i https://pypi.tuna.tsinghua.edu.cn/simple \
    -r requirements.txt

# ───── Stage 2: Production ─────
FROM python:3.11-slim-bookworm

# 安全：创建非 root 用户
RUN groupadd -r flask && useradd -r -g flask flask

# 安装运行时系统依赖
RUN apt-get update && apt-get install -y --no-install-recommends \
    libpq-dev curl && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /app

# 从 builder 复制已安装的 Python 包
COPY --from=builder /install /usr/local

# 复制应用代码
COPY --chown=flask:flask ./app ./app
COPY --chown=flask:flask ./migrations ./migrations
COPY --chown=flask:flask gunicorn_config.py .

# 切换到非 root 用户
USER flask

# 健康检查
HEALTHCHECK --interval=30s --timeout=3s --retries=3 \
    CMD curl -f http://localhost:8000/health || exit 1

EXPOSE 8000

# 启动命令
CMD ["gunicorn", "-c", "gunicorn_config.py", "app:app"]
```

### 10.2 docker-compose 完整配置

```yaml
# docker-compose.yml
version: '3.8'

x-common-env: &common-env
  FLASK_ENV: production
  TZ: Asia/Shanghai

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flask-web
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      <<: *common-env
      DATABASE_URL: postgresql://flask:password@db:5432/flask_db
      REDIS_URL: redis://redis:6379/0
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./uploads:/app/uploads
    networks:
      - app-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  web-worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flask-worker
    restart: unless-stopped
    command: celery -A app.celery_app worker -Q default,high -c 2
    environment:
      <<: *common-env
      DATABASE_URL: postgresql://flask:password@db:5432/flask_db
      REDIS_URL: redis://redis:6379/0
    depends_on:
      - db
      - redis
    networks:
      - app-network

  db:
    image: postgres:15-alpine
    container_name: flask-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: flask
      POSTGRES_PASSWORD: password
      POSTGRES_DB: flask_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U flask -d flask_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-network

  redis:
    image: redis:7-alpine
    container_name: flask-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 256mb
                          --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - app-network

  nginx:
    image: nginx:1.25-alpine
    container_name: flask-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/ssl:ro
    depends_on:
      - web
    networks:
      - app-network

volumes:
  postgres_data:
  redis_data:

networks:
  app-network:
    driver: bridge
```

---

## 第十一部分：健康检查与优雅关闭

### 11.1 Flask 健康检查端点

```python
# app/health.py
from flask import Blueprint, jsonify
from app import db
import redis
import time

health_bp = Blueprint("health", __name__)

start_time = time.time()

@health_bp.route("/health")
def health_check() -> tuple[dict, int]:
    """基础健康检查（负载均衡器用）"""
    return {"status": "ok", "timestamp": time.time()}, 200

@health_bp.route("/health/ready")
def readiness_check() -> tuple[dict, int]:
    """就绪检查：检查依赖是否可用"""
    checks = {}
    healthy = True

    # 数据库检查
    try:
        db.session.execute(db.text("SELECT 1"))
        checks["database"] = "ok"
    except Exception as e:
        checks["database"] = f"error: {e}"
        healthy = False

    # Redis 检查
    try:
        r = redis.from_url(current_app.config.get("REDIS_URL", "redis://localhost:6379"))
        r.ping()
        checks["redis"] = "ok"
    except Exception as e:
        checks["redis"] = f"error: {e}"
        healthy = False

    status_code = 200 if healthy else 503
    return {
        "status": "ready" if healthy else "not ready",
        "checks": checks,
        "uptime": time.time() - start_time,
    }, status_code

@health_bp.route("/health/live")
def liveness_check() -> tuple[dict, int]:
    """存活检查（Kubernetes liveness probe）"""
    return {
        "status": "alive",
        "uptime": time.time() - start_time,
    }, 200
```

### 11.2 优雅关闭

```python
# app/__init__.py
import signal
import sys
from flask import Flask

def create_app() -> Flask:
    app: Flask = Flask(__name__)

    # 注册优雅关闭处理
    _register_shutdown_handlers(app)

    return app

def _register_shutdown_handlers(app: Flask) -> None:
    """注册信号处理器实现优雅关闭"""

    def graceful_shutdown(signum, frame):
        app.logger.info("收到关闭信号，开始优雅关闭...")

        # 1. 停止接受新请求（Gunicorn 会处理）
        # 2. 等待正在处理的请求完成
        # 3. 关闭数据库连接
        try:
            db.session.remove()
            db.engine.dispose()
            app.logger.info("数据库连接已关闭")
        except Exception as e:
            app.logger.error(f"关闭数据库连接失败: {e}")

        # 4. 关闭 Redis 连接
        try:
            from app.extensions import redis_client
            redis_client.close()
            app.logger.info("Redis 连接已关闭")
        except Exception as e:
            app.logger.error(f"关闭 Redis 连接失败: {e}")

        app.logger.info("优雅关闭完成")
        sys.exit(0)

    signal.signal(signal.SIGTERM, graceful_shutdown)
    signal.signal(signal.SIGINT, graceful_shutdown)

    @app.teardown_appcontext
    def shutdown_session(exception=None):
        """请求结束时清理 session"""
        db.session.remove()
```

---

## 第十二部分：常见坑点排查

### 12.1 Gunicorn Worker 超时

**错误信息：**

```
[CRITICAL] WORKER TIMEOUT (pid:12345)
```

**根因分析：**

Gunicorn 默认 30 秒超时。如果请求处理超过 30 秒，Worker 会被强制 kill。常见于慢查询、大文件处理、外部 API 调用。

```python
# Gunicorn 配置
timeout = 30  # 默认

# 增加超时
timeout = 120

# 或者改为异步 Worker
worker_class = "gevent"
timeout = 120

# 但更好的做法：将耗时操作转为异步任务
# 不要阻塞 Web Worker
@app.route("/export-report")
def export_report():
    task = export_report_task.delay(user_id=current_user.id)
    return {"task_id": task.id}, 202  # 立即返回
```

### 12.2 测试中 app 状态污染

**错误信息：**

```
AssertionError: assert 'dev-secret' == 'test-secret'
```

**根因分析：**

session 级 fixture 的 `app` 被前面的测试修改了配置，后面测试共享了同一个 app 实例。

**修复：**

```python
# conftest.py — 每次测试重置 app 配置
@pytest.fixture
def app():
    app = create_app("testing")
    ctx = app.app_context()
    ctx.push()
    yield app
    ctx.pop()

# 或使用 function 级 scope（安全但慢）
@pytest.fixture
def client(app):
    with app.test_client() as c:
        yield c
```

### 12.3 问题排查表

| 现象 | 可能原因 | 检查方法 | 解决方案 |
|------|---------|---------|---------|
| Worker TIMEOUT | 请求处理超时 | 检查慢查询日志 | 增加 timeout 或使用异步任务 |
| Nginx 502 Bad Gateway | Gunicorn 未启动或崩溃 | `systemctl status flask-app` | 启动 Gunicorn，检查配置 |
| 测试间数据污染 | 事务未回滚 | 打印测试前后的数据 | 使用事务回滚 fixture |
| Docker 镜像过大 | 构建依赖未清理 | `docker images` | 多阶段构建 + `--no-cache-dir` |
| 静态文件 404 | Nginx 配置错误 | `nginx -t` | 检查 alias 和 root 路径 |
| 内存持续增长 | Worker 内存泄漏 | `ps aux --sort=-%mem` | 设置 max_requests 定期重启 Worker |

---

## 第十三部分：调试与排错实战

### 13.1 调试会话：测试通过但生产报错

```
场景：测试全部通过，部署到生产后部分 API 返回 500。

Step 1: 检查生产日志
/var/log/flask/error.log:
  File "/app/api/articles.py", line 42, in get_articles
    articles = Article.query.paginate(page=page, per_page=20)
  OperationalError: (sqlite3.OperationalError) no such table: articles

Step 2: 对比测试和生产配置
# 测试环境: SQLALCHEMY_DATABASE_URI = "sqlite:///:memory:"  (自动建表)
# 生产环境: SQLALCHEMY_DATABASE_URI = "postgresql://..."   (需要 migration)

Step 3: 检查生产启动脚本
服务器启动脚本遗漏了 `flask db upgrade`

Step 4: 修复
# 在启动脚本或 Docker ENTRYPOINT 中添加
flask db upgrade
gunicorn app:app

# 或者在 create_app 中自动检查
def create_app():
    app = Flask(__name__)
    # ...
    with app.app_context():
        from flask_migrate import upgrade
        upgrade()  # 自动运行迁移
```

### 13.2 调试工具：请求追踪中间件

```python
import uuid
import time
from flask import request, g

def init_request_tracing(app: Flask) -> None:
    """为每个请求添加追踪 ID"""

    @app.before_request
    def start_trace():
        g.trace_id = request.headers.get("X-Request-ID", str(uuid.uuid4()))
        g.request_start = time.time()

    @app.after_request
    def end_trace(response):
        response.headers["X-Request-ID"] = g.get("trace_id", "unknown")
        elapsed = time.time() - g.get("request_start", time.time())
        response.headers["X-Response-Time"] = f"{elapsed:.4f}s"

        app.logger.info(
            f"[{g.get('trace_id', '?')}] "
            f"{request.method} {request.path} "
            f"{response.status_code} ({elapsed:.4f}s)"
        )

        return response
```

| 进阶知识点 | 说明 |
|-------------|------|
| 测试事务回滚 | 测试间共享 session，测试后自动回滚 |
| Factory 测试数据 | factory_boy 快速生成测试数据 |
| Mock 外部服务 | requests-mock, unittest.mock |
| 测试金字塔 | E2E → 集成 → 单元测试比例 |
| Gunicorn配置 | workers/threads/timeout/graceful |
| Nginx 反向代理 | upstream/SSL/限流/静态文件 |
| Docker 多阶段 | builder + production 分离 |
| 健康检查 | /health /health/ready /health/live |
| 优雅关闭 | SIGTERM 处理 + 连接关闭 |
| 请求追踪 | X-Request-ID 全链路追踪 |