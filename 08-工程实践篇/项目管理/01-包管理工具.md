# 包管理工具

本章介绍 Python 主流包管理工具的使用。

---

## pip 包管理器

pip 是 Python 官方推荐的包管理工具。

### 基本命令

```bash
# 安装包
pip install package_name

# 安装指定版本
pip install package_name==1.0.0

# 升级包
pip install --upgrade package_name

# 卸载包
pip uninstall package_name

# 列出已安装的包
pip list

# 导出依赖
pip freeze > requirements.txt

# 从文件安装依赖
pip install -r requirements.txt
```

### 虚拟环境

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境 (macOS/Linux)
source venv/bin/activate

# 激活虚拟环境 (Windows)
venv\Scripts\activate

# 退出虚拟环境
deactivate
```

---

## uv 包管理器

uv 是新一代 Python 包管理器，用 Rust 编写，速度极快。

### 安装

```bash
# macOS/Linux
curl -LsSf https://astral.sh/uv/install.sh | sh

# Windows
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

### 基本使用

```bash
# 初始化新项目
uv init my-project
cd my-project

# 添加依赖
uv add requests
uv add flask@2.0.0

# 添加开发依赖
uv add --dev pytest black ruff

# 运行脚本
uv run python main.py

# 创建虚拟环境
uv venv
```

### 项目结构

```
my-project/
├── pyproject.toml    # 项目配置和依赖
├── uv.lock          # 锁定文件
├── .venv/           # 虚拟环境
└── src/
    └── my_project/
        └── __init__.py
```

---

## conda 包管理器

conda 是跨平台的包管理和环境管理工具，适合数据科学项目。

### 基本使用

```bash
# 创建环境
conda create -n myenv python=3.11

# 激活环境
conda activate myenv

# 安装包
conda install package_name

# 从 conda-forge 安装
conda install -c conda-forge package_name

# 导出环境
conda env export > environment.yml

# 从文件创建环境
conda env create -f environment.yml
```

---

## 包管理器对比

| 特性 | pip | uv | conda |
|------|-----|----|-------|
| 速度 | 慢 | 极快 | 中等 |
| 虚拟环境 | 需配合 venv | 内置 | 内置 |
| 非 Python 包 | ❌ | ❌ | ✅ |
| 适用场景 | 通用 | 现代 Python 项目 | 数据科学/ML |

---

## 本章小结

```
┌─────────────────────────────────────────────────────────────┐
│                    包管理工具 知识要点                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   pip：                                                      │
│   ✓ Python 官方包管理器                                     │
│   ✓ 配合 venv 使用虚拟环境                                  │
│                                                             │
│   uv：                                                       │
│   ✓ 新一代包管理器，速度极快                                │
│   ✓ 内置虚拟环境管理                                        │
│                                                             │
│   conda：                                                    │
│   ✓ 跨平台包管理器                                          │
│   ✓ 适合数据科学项目                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```